# Implementation Acceptance Matrix — Yash Trade (brief 2026-09-20, R00–R17)

Maintained during the run. States per surface: `NOT STARTED` · `IN PROGRESS` · `IMPLEMENTED - NOT VERIFIED` · `VERIFIED` · `BLOCKED` · `NOT APPLICABLE (reason)`.
`VERIFIED` is never given from code inspection alone. A web-preview pass never implies an Android/iOS pass.

Baseline (recorded before editing, 20 Sep 2026):
- App/backend repo HEAD `0530ebd5753eb75c9b3f489aefe60013c368f178` (dirty tree carried from the previous session: backend `auth/catalog/commerce/core/install/media_lifecycle/pdf_authoring/pdf_jobs/people/queries/readiness/server.py`, new `discovery.py`, `notifications.py`, `queue_reset.py`, three new pytest files; frontend `_layout`, `(tabs)/_layout`, `index`, `login`, `verify-otp`, `api.ts`, `AuthContext.tsx`, `app.json`, `package.json`, new `src/navigation.ts`, `src/push.ts`). Nothing overwritten.
- Website repository: **no remote, checkout, credentials or project access exist in this environment** (`git remote -v` empty, no website env variables). All R16 rows are therefore BLOCKED (handoff only).
- Root `/app/package-lock.json` (82 bytes, `"packages": {}`) confirmed an empty accidental artifact → removed. `frontend/package-lock.json` (npm lockfile in the Yarn-only project, contradicting settled decision #1) → `git rm --cached`, ignored via `.gitignore`. `frontend/yarn.lock` preserved.
- Baseline pytest `tests/shared`: 104 passed then **1 failure** `test_refresh_rotation_and_reuse_revokes_family` (expects 401 on immediate replay; new 30 s refresh-race grace returns 409) — see R01-B. Baseline `tsc`: 1 error in `src/__tests__/dataCache.test.ts` (pre-existing).

**Current state — 21 September 2026 (after the independent review F01–F09, the follow-up review G01–G04 and the recheck G01c/G04b/L01).** Evidence below names the exact test, report or screenshot. "Browser" = Expo web preview (react-native-web) driven by Playwright against the live preview backend on the isolated store-review scope (`review__*` collections, synthetic reviewer accounts, SMS simulated, no push devices). Browser evidence is NOT native evidence: every AND/IOS cell that says IMPLEMENTED - NOT VERIFIED has web evidence at most and still needs a store build on a device. Git commits are the owner's ("Save to GitHub"); no commit hashes are recorded by the agent.

Surfaces: **AND** = app Android, **IOS** = app iOS, **BE** = backend, **WEB** = website (BFF/UI). Native columns can only reach VERIFIED with native runtime evidence (device/simulator), which this container does not have.

| ID | Sub-requirement | AND | IOS | BE | WEB | Files / tests / evidence (21 Sep 2026) | Remaining blocker |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R00-A | Staff phone preview/commit, customer promotion, same-key replay, partial-write recovery, stale preview binding, owner protection | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `shared/people.py`, `src/components/panel/StaffPhoneChange.tsx`; pytest `test_staff_phone_change.py` (12), `test_operations_iteration.py`, `test_shared_auth_identity.py` (5); browser iteration 37 B7 (`staff-phone-done` stays visible after commit) | Native UI run; website access |
| R00-B | International formatting/paste, call/WhatsApp links normalised | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `src/phone.ts`, `PhoneField.tsx`, `queries.contact_links`; Jest `phone.test.ts`, `phoneField.test.tsx` | Native paste test; provider delivery only observable with a real SMS (not run) |
| R00-C | Thumbnail-first, bounded/private-safe cache, invalidation, dry-run backfill, cold/warm measurements | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | NOT APPLICABLE (native media stack) | `src/dataCache.ts`, `src/api.ts` variants, `ProtectedMedia.tsx`, `commerce.py /files`, `media_cache.py`; Jest `dataCache.test.ts`, `productImage.test.ts`; pytest `test_media_cache_policy.py` (5), `test_backfill_thumbnails.py` (3); browser iteration 37 B5 (review-scope media 200 to `<img>`) | Device cold/warm measurement not possible here |
| R01-A | Login/OTP removed from back history; Home back stays; other screens back; deep link fallback to role home | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | BLOCKED | `src/navigation.ts` (`homeRouteFor`, `useSafeBack`, `useRootBackHandler` bound to focus — F09), `app/(tabs)/_layout.tsx`; Jest `rootBackHandler.test.tsx`, `navigationSession.test.tsx` | Hardware back can only be exercised natively (environment limitation, documented) |
| R01-B | Durable session, offline/5xx keeps credentials, single-flight refresh, race grace, replay revocation, expiry, logout + revocation; generation-bound isolation of a previous account at every async boundary (F01, G01, G01c) | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `shared/auth.py` (`rotate_refresh`, `REFRESH_RACE_SECONDS`, `SESSION_IDLE_DAYS`), `src/api.ts` (`storeCredentials`, `responseJson(res, epoch)`), `src/context/AuthContext.tsx`; pytest `test_session_family_slides_on_refresh_and_refresh_race_is_not_theft`, `test_shared_auth_identity.py`; Jest `lateRefresh.test.ts` (16: F01 ×6, G01 A–E + control, G01c F/G/H + control), `authSessionWeb.test.tsx` (6); live API iteration 38 C8 (G01 session isolation); browser iteration 39 C5 (401 → in-place sign-out) | Native SecureStore path exercised with doubles only; native run pending |
| R02 | Cross-platform pinch/pan viewer from every photo entry point; image switching resets; thumbnail while HD loads | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | BLOCKED | `src/components/ZoomableImage.tsx` (gesture-handler + reanimated), `app/image-viewer.tsx`; Jest `zoomableImage.test.tsx`; browser iteration 37 B5 (double-click zoom state Not zoomed → Zoomed in) | Real pinch gesture needs a device |
| R03 | Unseen-first Home refresh, real viewability, stable paging, exhaustion fallback, account isolation (F05, G03) | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `shared/discovery.py` (indexed catalogue walk + per-account cursor), `src/hooks/useDiscovery.ts` (60 % / 1.2 s viewability, batched impressions), `app/(tabs)/index.tsx`; pytest `test_discovery_sessions_unseen_first_stable_pages_and_isolation`, `test_f05_…`, `test_g03_…` (9,001 products); Jest `useDiscovery.test.tsx`; **browser iteration 39-B C6**: session 1 unseen 9 / seen 0 → 3 impressions posted only after ≥ 60 % for ≥ 1.2 s (no duplicates, same session id) → session 2 unseen 6 / seen 3, seen products last | FlatList viewability on native not run |
| R04 | Calculator continuous decimal typing, stable field identity, iOS Done | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | NOT APPLICABLE | `app/(tabs)/calculator.tsx` (`InputField` hoisted, `returnKeyType="done"`); Jest `calculator.test.tsx`; browser iteration 35 J8 | iOS Done key needs a device |
| R05 | Silver INR/kg & gold INR/10 g display/edit, canonical INR/g, conversions, old-client compat | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `shared/commerce.py` (`MCX_DISPLAY_UNITS`, `mcx_display`), `app/staff-rates.tsx`, rate list; pytest `test_mcx_unit_conversions_are_deterministic`, `test_mcx_display_units_round_trip_and_calculated_physical`; Jest `staffRates.test.tsx`; browser iteration 37 B9 | Native run |
| R06 | Keyboard avoidance on every input screen family + critical modals | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | BLOCKED | `KeyboardProvider` in `app/_layout.tsx`; `react-native-keyboard-controller` / KeyboardAvoidingView in 7 files (`login`, `verify-otp`, `calculator`, `admin-notifications`, staff `Controls`, …); lockfile record present since 21 Sep (L01) | Native keyboard-open screenshots impossible here (environment limitation, documented) |
| R07 | Four-box OTP autofill/paste/manual/backspace, single submission | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | BLOCKED | `app/verify-otp.tsx` (one logical value distributed over four boxes, `textContentType="oneTimeCode"`, `autoComplete sms-otp / one-time-code`, no `maxLength` truncation); Jest `verifyOtp.test.tsx` | OS suggestion acceptance needs a device |
| R08 | Server-driven 15 s resend across every purpose; abuse limits retained | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `shared/auth.py` (`RESEND_COOLDOWN_SECONDS=15`, `resend_timing`, atomic issuance under lock); pytest `test_otp_cooldown_is_15_seconds_server_driven_and_atomic` (first run 20 Sep FAILED on the concurrent second tap — fixed with the issuance lock, green since 21 Sep 07:42); `verify-otp.tsx` counts down from the server `resend_at` | Real SMS never sent in tests |
| R09-A | Admin notification composer: title/body/image/link/preview/audience/count/test/draft/send/history | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `app/admin-notifications.tsx`, `shared/notifications.py` admin routes; pytest `test_campaigns_audience_permissions_and_durable_delivery`, `test_g04b_*` (5), `test_notification_composer_apis_accept_both_administrator_paths_and_keep_scopes_apart` (ordinary admin AND review admin 200, telecaller/customer/review non-admins 403, scopes never see each other's campaigns); **browser iteration 39 C7** via the panel Notifications tab: filters 200 → inspect (6 users / 0 devices) → draft → confirm → send 200 `status=sent` → history row → customer inbox shows the row. No Jest for the screen itself | Native run; a push device only exists in a store build |
| R09-B | Permission prompt, marketing preference, token lifecycle, logout unlink, deep links, inbox | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `src/push.ts`, `app/notifications.tsx`, `/api/notifications/{devices,devices/unlink,preferences,inbox,…}`; pytest F02/G04 suites (device register/unlink, opt-out); browser iteration 37 B1 (staff Alerts → `/notifications`), 38-B C1 (staff opens a query from its notification row), 39 C7 (customer inbox row) | Expo push token registration only on a device build |
| R09-C | Real image push Android + iOS (Notification Service Extension) | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | `app.json` plugins `expo-notifications`, `expo-rich-notifications`; scratch prebuild 20 Sep produced `NotificationServiceExtension`; `message_for` sets `richContent.image`, `data.image`, `mutableContent` | **BLOCKED**: APNs/FCM credentials, `EXPO_PUSH_ACCESS_TOKEN`, store build, test devices — a provider-accepted push is not a displayed image |
| R09-D | Durable outbox, retries, receipts, invalid-token cleanup, no unauthorized audience (F02, F06, G04, G04b) | NOT APPLICABLE | NOT APPLICABLE | VERIFIED | NOT APPLICABLE | `notifications.py` (`enqueue`, `fan_out` frozen events + stable chunk keys, `eligible_messages`, `process_job` per-sub-request tickets, `recover_campaigns`, `check_receipts`); pytest `test_f02_…`, `test_f06_…` (2), `test_g04a_…` (2), `test_g04b_…` (3 + 2) | Real provider receipts only with a real Expo push token |
| R10-A | Upload Executive sign-in + every Contents/PDF capability inventoried and usable | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `core.content = allow(admin, upload_executive)` on catalog/pdf_jobs/pdf_authoring/media routes; reviewer account `store-review-upload`; pytest `test_upload_executive_matrix`; browser iteration 35 J1 (upload executive panel shows only Products/Content tools) | Native run |
| R10-B | Negative authorization across generic staff routes | NOT APPLICABLE | NOT APPLICABLE | VERIFIED | BLOCKED | pytest `test_role_route_sweep.py` (every registered route × every role), `test_upload_executive_matrix` | — |
| R11-A | One central pending workspace, all-time pagination, filters, full detail | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `src/components/staff/RequestsWorkspace.tsx`, `RequestDetail.tsx`, `queries.listing/detail`; pytest `test_central_queries.py` (3); Jest `requestsWorkspaceAlerts.test.tsx`, `requestDetailContact.test.tsx`; browser iterations 35 J2/J3a, 38-B C1, 39 C3 | Native run |
| R11-B | Heads/notes/follow-ups preserved, create→complete journey, old telecaller inventory | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `queries.mutate` (heads, notes, follow-ups, complete/reopen), `telecaller.tsx` leads retained; browser iteration 38-B C1 (note/head/follow-up/outcome) and 39 C3 (claim → outcome → `request-complete` → `PATCH /api/requests/{id}` `action=complete` 200 → "Completed") | Native run |
| R12-A | Query notification wired to every creation path, durable retry (F06, G04) | NOT APPLICABLE | NOT APPLICABLE | VERIFIED | NOT APPLICABLE | `queries` creation paths set `notify_state=pending`, `notifications.query_created` (frozen audience, completes partial fan-outs), worker `reconcile_pending_notifications`; pytest `test_f06_query_alert_is_never_lost_…`, `test_g04a_…` (2) | — |
| R12-B | Atomic first claim, ownership-enforced mutations, live updates | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `queries.claim` (single `find_one_and_update` predicate); pytest `test_central_queries.py`; live API iteration 38 C1 (parallel claims, one winner); browser iteration 35 J2 and 38-B C1 (`request-held-by-other` for the loser) | Native run |
| R13-A | Completed list, immutable completion events, IST daily/range report (F03, G02) | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `queries.insert_completion_row / settle_completion_ledger / reconcile_completion_ledger` (durable `ledger_pending` intents + cursor sweep), `GET /requests/reports/completions`, `CompletionReports.tsx` (reloads on focus and on "Today" — fix 21 Sep after iteration 39 showed a stale counter); pytest `test_f03_…`, `test_g02a_…` (2), `test_g02b_…`; **browser iteration 39 + 39-B**: report 8 → reopen (`PATCH action=reopen` 200) → 7 → re-complete → 8, ledger shows superseded rows | Native run |
| R13-B | 03:00 IST release of every non-terminal head, no cutoff, history retained (F04) | NOT APPLICABLE | NOT APPLICABLE | VERIFIED | NOT APPLICABLE | `shared/queue_reset.py` (`release_if_due` keyed on `claimed_at`, cycle + late sweep); pytest `test_daily_release.py` (4), `test_f04_…`; live: iteration 34 first FAILED (fixture `review-request-stale-0001` not released — worker did not iterate the review scope) → fixed → browser iteration 35 J0 (`stale-0002`: Unclaimed, chip "Released at 03:00 ×1", timeline event) | — |
| R13-C | Deployed scheduler, catch-up, multi-worker idempotency, races | NOT APPLICABLE | NOT APPLICABLE | IMPLEMENTED - NOT VERIFIED | NOT APPLICABLE | `install.py` starts `queue_reset.worker_loop`, `notifications.worker_loop`, PDF worker; `/api/health` `flows.queue_release` / `flows.push_notifications` expose `worker running`, cycle status, provider; preview health shows both workers running | Proof in the DEPLOYED process only after the owner publishes (health on production) |
| R14 | Admin/billing on the same central workspace, role-action matrix | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `c.operations` / `c.query_workers` gates; panel Requests/Reports tabs route to `/staff-requests` (duplicate inline list retired); pytest `test_role_route_sweep.py`; browser iteration 38-B C2 (admin reassignment, owner text), 39 C3 (admin reopen) | Native run |
| R15-A | Distinct disable / re-enable / delete for customers and staff, clear UI errors | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | `people.staff_disable`, `people.admin_delete`, `/customers/{uid}/delete`, `src/components/panel/AccountActions.tsx`, `app/delete-account.tsx`; pytest `test_disable_vs_delete_for_staff_and_customers`; browser iteration 37 B8, **39 C5** (disable → `PATCH /integrations/staff/{id}` 200 → telecaller's next call 401 → login screen in place → re-enable → fresh sign-in) and **39 C4** (customer self-deletion with step-up OTP, `profile_recreated=true` on the next reviewer sign-in) | Native run |
| R15-B | Sessions/tokens/assignments/erasure cleanup, protected owner/last admin, retry (F07) | NOT APPLICABLE | NOT APPLICABLE | VERIFIED | BLOCKED | `people.admin_delete` → `release_assignments`, `detach_devices`, `erase()` (completion-ledger scrub, unsent outbox dropped); pytest `test_f07_…`, `test_disable_vs_delete_…`, `test_owner_admin_bootstrap.py` (protection) | — |
| R16-A | Website code changes + compatibility tests | NOT APPLICABLE | NOT APPLICABLE | NOT APPLICABLE | BLOCKED | No website repository access in this environment | **WEBSITE IMPLEMENTATION/DEPLOYMENT BLOCKED** |
| R16-B | Shared contract/handoff, deploy order, website deployment status | NOT APPLICABLE | NOT APPLICABLE | VERIFIED | BLOCKED | `WEBSITE_HANDOFF.md`, `YASH_SHARED_API_CONTRACT.md`, `contracts/openapi.shared-v1.json` (130 paths) | Website deploy needs the website project; status unknown |
| R17-A | Store metadata / privacy / data-safety / reviewer-access docs | NOT APPLICABLE | NOT APPLICABLE | NOT APPLICABLE | NOT APPLICABLE | `APP_STORE_SUBMISSION.md` (iOS), `GOOGLE_PLAY_DATA_SAFETY.md`, `STORE_REVIEW_ACCESS.md`, `store_assets/` — written; owner items listed in `APP_STORE_SUBMISSION.md` §8 | Owner values (support URL, privacy URL, brand name) |
| R17-B | Authentic native screenshots + verified build IDs | BLOCKED | BLOCKED | NOT APPLICABLE | NOT APPLICABLE | Existing PNGs are Expo-web captures (disclosed) | No native build tooling/devices here |
| R17-C | Docs, final tests, commits, migrations/rollback, release-state report | IMPLEMENTED - NOT VERIFIED | IMPLEMENTED - NOT VERIFIED | VERIFIED | BLOCKED | This file, `RELEASE_READINESS.md`, `APP_STORE_SUBMISSION.md`, `memory/PRD.md` updated 21 Sep; indexes created by `core.ensure_indexes()` at startup (idempotent, no migration script needed; new collections `notification_events`, `discovery_cursors`, `maintenance_cursors`) | Commits are the owner's Save to GitHub (no agent Git actions) |

## Independent-review findings — status and evidence (F = review of 2093300, 20 Sep; G = follow-up review of e34d76a, 21 Sep; recheck of c3da84e, 21 Sep)
| Finding | Fix (files) | Evidence | State |
| --- | --- | --- | --- |
| F01 P1 old refresh response overwrites a newer account | `src/api.ts` session generation checked after every await; refresh bound per generation | Jest `lateRefresh.test.ts` "late refresh responses (F01)" 6/6; live API iteration 38 C8 | FIXED |
| F02 P1 queued pushes ignore logout/reassignment/disable/opt-out | `notifications.eligible_messages` re-validates token owner, account state, marketing preference, role before EVERY send | pytest `test_f02_queued_messages_are_revalidated_before_dispatch` | FIXED |
| F03 P1 partial completion loses the report record | durable `ledger_pending` intents settled on retry/detail/report/worker | pytest `test_f03_…`, `test_g02a_…` (2) | FIXED |
| F04 P1 post-03:00 edit evades the daily release | `queue_reset.release_if_due` keyed on `claimed_at`, inline on open/mutation, worker late sweep | pytest `test_f04_…`, `test_daily_release.py`; browser iteration 35 J0 (after the iteration 34 first failure, see R13-B) | FIXED |
| F05 P2 old unseen products never surface | `discovery.unseen_walk` over the whole catalogue | pytest `test_f05_…`, `test_g03_…` | FIXED |
| F06 P1 enqueue failure drops a query alert | `notify_state=pending` on every creation path + worker reconcile; campaigns never stuck in `sending` | pytest `test_f06_…` (2), `test_g04a_…` (2) | FIXED |
| F07 P2 erasure misses the completion ledger | `people.erase()` anonymises `request_completions` customer fields, drops unsent outbox messages | pytest `test_f07_…` | FIXED |
| F08 P2 Call/WhatsApp bypass the claim | `RequestDetail.tsx` contact gate (`request-contact-gate`): claim first, server re-check, disabled when held by another | Jest `requestDetailContact.test.tsx`; browser iteration 35 J3a, 38-B C1 | FIXED (web); native tap not run |
| F09 P2 root back handler bound to mount | `useRootBackHandler` uses `useFocusEffect` | Jest `rootBackHandler.test.tsx` | FIXED (unit); hardware back is native-only |
| G01 P1 isolation at every async boundary (HTTP, storage read, storage write) | serialized generation-bound `storeCredentials`; AuthContext login/logout use it | Jest `lateRefresh.test.ts` G01 A–E + control | FIXED |
| G01c P1 body parsed after the account changed | `responseJson(res, epoch)` rejects a late body (success, error, upload); AuthContext bootstrap/login/refreshUser continuations generation-bound | Jest G01c F/G/H + control (4), `authSessionWeb.test.tsx` "a late /auth/me result of account A never updates the state once account B signed in" | FIXED 21 Sep |
| G02 P1 interrupted reopen / bounded reconciliation | `ledger_pending` supersede intents + durable cursor sweep (`maintenance_cursors`) | pytest `test_g02a_…` (2), `test_g02b_…` (501 rows) | FIXED |
| G03 P2 rotation starvation | indexed catalogue walk with per-account continuation cursor | pytest `test_g03_…` (9,001 products, 3 sessions) | FIXED |
| G04 P1 partial fan-out / stranded campaign | `notification_events` frozen audience + stable chunk keys; campaign fan-out lease + worker resume | pytest `test_g04a_…` (2), `test_g04b_…` (3); browser iteration 39 C7 | FIXED |
| G04b P2 later provider sub-request failure resends an accepted one | `process_job` stores each sub-request's tickets (with token) before the next call; retry sends the unsent remainder only | pytest `test_g04b_later_sub_request_failure_…` (125 messages: 100 accepted, 25 retried, each device once), `test_g04b_retry_revalidates_only_the_unsent_remainder_…` | FIXED 21 Sep (network failure after remote acceptance stays ambiguous without provider idempotency — documented, not hidden) |
| L01 P2 lockfile lacks four direct dependencies | `frontend/yarn.lock` records `expo-device 8.0.10`, `expo-notifications 0.32.17`, `expo-rich-notifications 0.1.0`, `react-native-keyboard-controller 1.18.6` (+ transitive deps, no other resolution changed) | `yarn install --frozen-lockfile --ignore-scripts` exit 0: `test_reports/frozen_lockfile_install_2026-09-21b.txt` | FIXED in the working tree (owner's Save to GitHub publishes it); a native build remains the only proof of a working bundle |
| Browser-plan note (recheck) — "review-admin cannot open `/admin-notifications`" | No auth change: the page checks the shared admin role and every composer API uses `c.admin`; iteration 38-B had used `page.goto`, which drops the memory-only web session | live API: review-admin bearer 200 on `/api/admin/notifications/{filters,campaigns,outbox,audience}`, review-customer 403; pytest `test_notification_composer_apis_accept_both_administrator_paths_and_keep_scopes_apart`; browser iteration 39 C7 through `panel-tab-notifications` | NOT A DEFECT |

## Test-run log (command · environment · timestamp · result · artefact)
| # | Command | Env | Timestamp (UTC) | Result | Artefact |
| --- | --- | --- | --- | --- | --- |
| 1 | `pytest tests/shared -q -x` (baseline, dirty tree) | preview container, local mongod | 2026-09-20 | 104 passed, 1 failed (`test_refresh_rotation_and_reuse_revokes_family`: 409 ≠ 401) | `/tmp/pytest_baseline.log` |
| 2 | `npx tsc --noEmit` (baseline) | preview container | 2026-09-20 | 1 error `src/__tests__/dataCache.test.ts(45,12)` | `/tmp/tsc_baseline.log` |

## Notes / assumptions
- R01-B baseline failure: the new grace window (`REFRESH_RACE_SECONDS=30`) answers an immediate replay of a just-rotated refresh token with `409 REFRESH_IN_PROGRESS` and keeps the family alive so two concurrent app calls cannot revoke each other. Replay OUTSIDE the window must still revoke the family (401). The test will be re-specified to cover both cases with a fake clock rather than relaxed.
- Release states are tracked separately at the end of this file.

## Release states (kept separate, never merged)
| State | Value |
| --- | --- |
| Code Complete | IN PROGRESS |
| Tests Passed | IN PROGRESS |
| Native Verified | NOT VERIFIED (no device/simulator in this environment) |
| Website Deployed | BLOCKED (no website project access) |
| Build Uploaded | NOT DONE (owner: Publish → store builds) |
| Submitted for Review | NOT DONE |
| Store Live | NOT DONE |
