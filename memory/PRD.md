# Yash Trade — store-submission build, 13 September 2026 (`shared-v1-store-submission-2026-09-13`)

## STORE-SUBMISSION PREP — 13 Sep 2026 — DONE, tested (testing agent iterations 26/27 + 13 Sep follow-up)
User request: reviewer accounts (customer/admin/telecaller/billing) on isolated sample data, private owner-only screen to provision/rotate/revoke reviewer credentials, complete account deletion (user data, analytics, AI history, media), explicit AI consent with withdrawal, store questionnaire answers, website handoff text. Reviewer accounts must never touch production collections; keys shown once, stored as hashes; withdrawing AI consent stops transfers and deletes existing AI chat history.
- **Review isolation model CHANGED**: `review__*` prefixed collections in the SAME database (`REVIEW_ACCESS_ENABLED=true` in `backend/.env`, real value). `REVIEW_DB_NAME` / separate review DB / `test_review_storage_hotfix.py` REMOVED. Isolation is application-enforced by the signature-verified session scope (`scope=review` in the JWT, `review.`-prefixed refresh tokens): `core._PrefixedDatabase`, `core.collection_name()` for `$lookup`, scope-aware `db`/`store_object`/`send_sms`. `test_review_prefixed_storage.py` proves a `mongod --auth` user restricted to the main DB serves reviewer sign-in. Health: `flows.review {ready, optional, enabled, usable, storage: prefixed_collections, isolation: application_enforced, accounts_enabled}`, `configuration.REVIEW_ACCESS_ENABLED/REVIEW_STORAGE_USABLE`.
- **Owner console**: `shared/review_admin.py` → `GET /api/admin/review/status`, `POST /challenge` (OTP purpose `review_keys` to the owner's number, single use, 10 min), `POST /keys {action: provision|rotate|revoke|reset_data, reviewer_id?, otp, challenge_id, environment, api_base_url}`. Owner only (`is_owner`, `OWNER_ADMIN_PHONE`): reviewer admin → `403 REVIEW_SCOPE_FORBIDDEN`, other admin → `403 OWNER_ADMIN_REQUIRED`; 503 when review access disabled. Screen `frontend/app/review-keys.tsx` (Panel > "Store review" tab, hidden in review sessions): keys shown once, COPY per account, COPY PRIVATE NOTE, DOWNLOAD .TXT, PREVIEW/PRODUCTION badge from backend origin. CLI `tools/provision_review_access.py --expected-db <DB_NAME> …` + `Provision-ReviewAccess.ps1 -DbName` operate on the same accounts.
- **AI consent**: `shared/ai_consent.py` → `GET/POST /api/ai/consent {granted, source}`; `/api/ai/chat` → `403 AI_CONSENT_REQUIRED` without current consent; withdrawal deletes stored AI history, in-flight reply after withdrawal not stored. UI: `src/components/AiConsentCard.tsx` in `ai-assistant.tsx` (gate) and `app/ai-consent.tsx` (Profile → AI Data Sharing; withdraw).
- **Deep deletion**: analytics, AI history, consent, reports removed; `session_version` incremented → stale bearer `401 SESSION_REVOKED`; tombstone `deleted_identities` + outbox event. Review-scope OTP challenges disclose `simulated_otp` ONLY to the same authenticated review session (delete-account / phone-change); deleted reviewer profile recreated fresh on the next sign-in with an ENABLED key (`profile_recreated=true`).
- **13 Sep fix (iteration 27 residual)**: root cause was NOT in `review-keys.tsx` — on web, a React Navigation history reset (`popstate` to a URL outside its stack) REMOUNTS the root layout and therefore `AuthProvider`; the web session is memory-only so `user` became null → `/login`. `src/context/AuthContext.tsx` `tokenStore.get` on web now returns the module-level token (`getToken()`), so the remounted provider re-validates the session with `GET /auth/me`. Still memory-only (page reload → signed out), server-decided, no client role decision. Verified live on preview (reviewer admin → REVIEW_SCOPE_FORBIDDEN card via client-side nav; disposable non-owner admin → OWNER_ADMIN_REQUIRED card via panel tab + client-side nav; signed-out direct URL and hard reloads → `/login`) and by Jest `authSessionWeb.test.tsx` (5) + `reviewKeysAccessGate.test.tsx` (5). Owner positive path verified by Jest (mocked 200) + pytest `test_review_owner_console.py`, NOT by a live session on the real owner record (would need a real OTP or minting a session on 9999813334 — avoided).
- Tests: backend full shared suite `test_reports/pytest/store_submission_full_2026-09-13.xml` (116+ passed); Jest 8 suites / 53 tests; `tsc` clean; lint 0 errors. Live preview automated: `test_reports/iteration_26.json`, `iteration_27.json` (+ prod-snapshot JSONs: unprefixed collection counts unchanged).
- Docs updated 13 Sep: STORE_REVIEW_ACCESS.md (procedure A = owner console, B = CLI with `-DbName`/`--expected-db`; evidence), PRODUCTION_ADMIN_RECOVERY.md (`REVIEW_ACCESS_ENABLED` row, health expectations, §4), RELEASE_READINESS.md (13 Sep section on top; older `REVIEW_DB_NAME` rows marked historical), WEBSITE_HANDOFF.md (13 Sep section: no new website obligation), `contracts/openapi.shared-v1.json` regenerated (127 paths), `build_release_handoff.py` status text, `memory/test_credentials.md`.
- Preview state after the work: all four preview reviewer accounts REVOKED (`accounts_enabled=0`); disposable fixtures deleted; no private notes left in the container.
- Owner next: Save to GitHub (or use the local commit) → republish → Secrets `STAFF_SERVICE_KEY`, `BUILD_COMMIT`, frontend `EXPO_PUBLIC_BACKEND_URL` (NO `REVIEW_DB_NAME`) → republish → `/api/health` (`build=shared-v1-store-submission-2026-09-13`, `flows.owner_admin.state=promoted`, `flows.review.ready=true`) → real OTP login as 9999813334 → Panel > Store review → PROVISION (one real OTP) → save note → four reviewer sign-ins → store forms.
- DO NOT: send OTPs to 9999813334 in automated tests; trigger owner-console write actions with a real owner session in tests; re-introduce `REVIEW_DB_NAME`.

# Previous — review-fonts build, 12 September 2026 (`shared-v1-review-fonts-2026-09-12`)

## DEFAULT OWNER ADMIN — 12 Sep 2026 (user: "Make 9999813334 as a default admin in both website and app"; choices 1a create-if-missing, 2a real OTP only, 3a every environment) — DONE, tested
- SUPERSEDES the earlier "no startup admin seed / operator-only recovery" rule for THIS ONE phone. `backend/.env` declares `OWNER_ADMIN_PHONE=9999813334` (real value, carried into every deployment). `backend/shared/owner_admin.py::ensure_owner_admin()` runs in `server.startup()` right after `ensure_indexes()`.
- Behaviour: single record with the phone (normalized field OR legacy formatting, same lookup as `by_phone`) → promoted to admin on the SAME record (`$set role/updated_at`, `$inc session_version`, `$push identity_events {type: owner_admin_bootstrap, actor_id: system:owner_admin_bootstrap, old_role}`, all session families revoked once); none → created as active admin (no password/OTP/token; `registration_source=owner_admin_bootstrap`); already admin → untouched (sessions kept). Refuses with no change: ≥2 records (`OWNER_ADMIN_IDENTITY_CONFLICT`), inactive/deleted record (`OWNER_ADMIN_ACCOUNT_INACTIVE`), tombstone in `deleted_identities` (`OWNER_ADMIN_DELETED_IDENTITY`), missing/placeholder/invalid phone (`OWNER_ADMIN_PHONE`), DB/config error (`OWNER_ADMIN_NOT_APPLIED`); RuntimeError inside review scope. Never touches other accounts.
- Website: role comes from the canonical record via the portal OTP exchange (`channel=portal` + `X-Staff-Service-Key`) — no website-local admin; website prompt/handoff updated accordingly.
- Protection: `people.last_admin_guard` → `409 OWNER_ADMIN_PROTECTED` on demote/disable/DELETE of the owner via `/integrations/staff/{ref}`; `people.erase()` refuses the owner record (self-deletion is customer-only anyway → 403 for admins).
- Health: `flows.owner_admin {ready, issues, state: created|promoted|already_admin|None, phone_suffix, detail}` (NON-optional → part of `ready`), `configuration.OWNER_ADMIN_PHONE`, capability `owner_admin_bootstrap`; `DEPLOY_ENV_KEYS` lists OWNER_ADMIN_PHONE. Preview: the legacy record `bcdf18c9-…` (phone only, no phone_normalized) was promoted customer→admin by the first reload; now `already_admin`.
- Tests: `tests/shared/test_owner_admin_bootstrap.py` 5/5; hotfix suite asserts the restricted-user process creates the owner admin in the main DB; full shared suite 109 passed / 5 skipped (`test_reports/pytest/owner_admin_full_2026-09-12.xml`). Integration playbook (admin bootstrap) followed: promote only on a real transition, revoke only that user's sessions, unique phone index guards concurrent creation.
- Docs: PRODUCTION_ADMIN_RECOVERY (Boundary, §1 row, §3 automatic + manual alternative, §5, §6, safeguards), WEBSITE_AUTH_FIX_PROMPT, WEBSITE_HANDOFF, YASH_SHARED_API_CONTRACT (health shape), RELEASE_READINESS, backend/.env.example.
- Owner next: Save to GitHub → republish → `/api/health` shows `flows.owner_admin.state=promoted` in production → set website settings → one real OTP login per surface as 9999813334 (app → /panel admin; website → admin UI).

## PRODUCTION HOTFIX — 12 Sep 2026 (after the first deployment attempt) — DONE, tested (iteration 24)
- Cause: production MongoDB user is authorised for the main DB only; `REVIEW_DB_NAME=jewellers_app_review` (inherited from preview `.env`) rejected `createIndexes` (code 13) → backend crashed at startup.
- Fix (`core.initialize_review()`, `install.py` startup hook, `readiness.py`, `review.py`): the OPTIONAL review DB is proven (ping + indexes) or marked unusable for the process. Startup continues; `/api/health` `flows.review={ready:false, issues:[REVIEW_DB_UNAUTHORIZED|REVIEW_DB_UNAVAILABLE|REVIEW_DB_NAME], optional, configured, usable, detail}`, `configuration.REVIEW_DB_USABLE`; reviewer login + existing review sessions/refresh → 503 `REVIEW_UNAVAILABLE`; `core.scopes()` gives workers only usable scopes (no re-polling); review DB never created, main DB untouched; one sanitized warning. PRIMARY DB failure still aborts startup; `REVIEW_DB_NAME == DB_NAME` still refuses to start.
- **Decision (user "Proceed")**: `backend/.env` keeps `REVIEW_DB_NAME=SET_IN_PUBLISH_SECRETS` (supersedes decision 5 below) → no review DB in preview or any deployment until the owner sets a real, distinct, AUTHORISED name in Secrets; preview reviewer login answers 503 by design.
- CLI `--verify-note` (decision: strict): pre-flight compares the note's recorded `Backend:` URL with `--api-base-url` (scheme, host, effective port, path) BEFORE any request → exit 1 `blocked` ("pins backend …"); a note with `Backend: (not recorded)` is refused (re-issue with `--rotate … --api-base-url … --write-note`); transport failure AFTER a valid pre-flight → exit 2 `verification_incomplete` + sanitized `NOT COMPLETED - <ErrorType>` entry appended to the note. Windows wrapper help updated.
- Git: `frontend/yarn.lock` had never been staged (release-pass commit deleted `package-lock.json` only; platform save commits tracked files only) → now `git add`ed and committed locally with the hotfix (user chose local commit). `build_release_handoff.py` derives every hash from the Git blobs of HEAD (`git_blob_sha1` + sha256 of blob bytes per file, `tracked_lockfile`, whole-tree digest, `source_commit.sha`), refuses dirty tree / untracked lockfile / non-Yarn lockfiles.
- Tests: `test_review_storage_hotfix.py` 7/7 (real `mongod --auth`, restricted user), CLI 8/8 (new URL-pin + exit-2 cases), full shared suite 104 passed / 5 skipped (`test_reports/pytest/hotfix_full_2026-09-12.xml`); testing agent iteration 24: live preview health/503/404 semantics + 25/25 regression + CLI exit codes reproduced (`test_reports/iteration_24.json`). Flaky fixed-window rate-limit test made boundary-safe (test-only).
- Docs updated: RELEASE_READINESS, STORE_REVIEW_ACCESS, PRODUCTION_ADMIN_RECOVERY, WEBSITE_HANDOFF, backend/.env.example.
- Owner next (unchanged order): Save to GitHub → verify source commit SHA on GitHub → republish → Secrets (`REVIEW_DB_NAME` must be a DB the deployment's user may readWrite; `STAFF_SERVICE_KEY`, `BUILD_COMMIT`, frontend `EXPO_PUBLIC_BACKEND_URL`) → republish → `/api/health` (`flows.review.issues=["REVIEW_DB_UNAUTHORIZED"]` = grant rights, app keeps serving) → recovery → reviewer provisioning → production OTP test.

## SETTLED OWNER DECISIONS (12 Sep 2026, final release pass) — DO NOT RE-ASK after any context summary
1. **Package manager = Yarn** (as declared in `frontend/package.json#packageManager`). `frontend/yarn.lock` is the single committed lockfile; `frontend/package-lock.json` is removed. Clean-checkout install/tests (`yarn install --frozen-lockfile`, `yarn test`, `tsc`, lint) are the evidence, not the existing node_modules.
2. **`frontend/app/review-access.tsx` MUST be tracked.** Root `.gitignore` rule `review-access*` was the cause; it is narrowed to private credential notes (`*review-access*.txt`) with an explicit `!frontend/app/review-access.tsx`. The reviewer login screen and all four role destinations are tested, not only the link.
3. **`WEBSITE_RELEASE_HANDOFF.zip` contents approved**: WEBSITE_HANDOFF.md, YASH_SHARED_API_CONTRACT.md, WEBSITE_AUTH_FIX_PROMPT.md, RELEASE_READINESS.md, STORE_REVIEW_ACCESS.md, PRODUCTION_ADMIN_RECOVERY.md, IDENTITY_EXPORT_CONTRACT.md, contracts/openapi.shared-v1.json + HANDOFF_MANIFEST.json (per-file sha256, schema checksum, build identity, deployment/test status; pre-change baseline commit `6a6cddd`/`e14854b` clearly distinguished from the final source tree — the pre-change commit is NEVER labelled as the completed implementation). Excludes .env, credentials, OTPs, private notes, customer data.
4. **Real OTP: ONE preview SMS to 9999813334 is AUTHORISED and to be sent by the agent** from the configured preview backend through the normal `POST /api/auth/send-otp` login flow with the existing MSG91 provider — no identity change, no validation bypass, no demo OTP. Labelled PREVIEW SMS TEST; dispatch ≠ receipt/verification/production admin login. If dispatch is blocked, report the actual blocker and continue. Do not request this permission again. Production OTP/admin verification remains an owner step after production configuration + recovery.
5. **`backend/.env` responsibility is the AGENT's**: `STAFF_SERVICE_KEY`, `REVIEW_DB_NAME`, `BUILD_COMMIT` are declared in the real preview `backend/.env` (names only reported). `STAFF_SERVICE_KEY=SET_IN_PUBLISH_SECRETS` (non-working, <32 chars, fails closed), `BUILD_COMMIT=SET_IN_PUBLISH_SECRETS` (reported as unrecorded), `REVIEW_DB_NAME=jewellers_app_review` (working preview review DB, distinct from `DB_NAME=jewellers_app`; production uses a different real name). Sequence: declare in .env → republish registers the names → owner enters real values privately in the existing-key editor → republish again.
6. **CLI exit codes are tested, not just documented**: 0 = done (+ every key proven when verification requested), 1 = blocked before any change, 2 = keys issued/kept but verification FAILED or INCOMPLETE. stdout JSON `outcome` carries the run result; `status` stays the account table. `--verify-note` (read-only, no DB) is available in the CLI and the Windows wrapper (`-VerifyNote`).
7. **Release backend origin**: `frontend/app.config.js` resolves `extra.backendUrl` to `https://yash-tryon-test.emergent.host` whenever `NODE_ENV=production` (Publish web export / store builds) and `EXPO_PUBLIC_BACKEND_URL` is empty or a `*.preview.emergentagent.com` origin; `frontend/.env` declares `EXPO_PUBLIC_PRODUCTION_BACKEND_URL`. Support confirmed the platform does NOT rewrite EXPO_PUBLIC_* on publish and existing deployment Secrets are not overwritten → the owner must also set the deployment Secret `EXPO_PUBLIC_BACKEND_URL=https://yash-tryon-test.emergent.host`. Protected preview .env URLs untouched.
8. **Never overclaim**: readiness booleans = configuration only; `sms_delivery_verified`/`account_role_verified` are always false in `/api/health`; a website readiness `false` = missing/invalid setting there, not backend unreachability; keys never grant admin — admin login is "restored" only after authenticated app + website sessions return the same canonical ID with role=admin after the §3 recovery.

## Release pass — DONE 12 Sep 2026 (this session)
- CLI: `outcome`/`status` collision fixed (was breaking `--status`), `--verify-note` (read-only, header pins environment + review DB, refuses mixed flags), strict logout + reuse checks, exit 0/1/2 proven by `test_cli_verify_against_live_backend_returns_strict_exit_codes` (real uvicorn subprocess). `Provision-ReviewAccess.ps1`: `-VerifyNote`, production requires `-NotePath`, exit-code messages, no connection-string prompt for verify-note.
- Placeholder proof: `tests/shared/test_placeholder_configuration.py` (backend process started with `SET_IN_PUBLISH_SECRETS` for REVIEW_DB_NAME/STAFF_SERVICE_KEY/BUILD_COMMIT → review 503, staff 503 CONFIGURATION_REQUIRED, commit "unrecorded", no DB created; CLI exit 1 before Mongo).
- `.env`: backend `REVIEW_DB_NAME=jewellers_app_review` restored (working preview review DB; previous agent had left a placeholder which disabled preview review login), `STAFF_SERVICE_KEY`/`BUILD_COMMIT` placeholders kept; frontend `EXPO_PUBLIC_PRODUCTION_BACKEND_URL` added. Names only reported.
- Packaging: root `.gitignore` `review-access*` → `*review-access*.txt|log|bak` + `!frontend/app/review-access.tsx`; `!/WEBSITE_RELEASE_HANDOFF.zip` (zips are otherwise ignored); `frontend/package-lock.json` deleted; README → Yarn; pre-existing lint error in `SmsDiagnostics.tsx` fixed. Clean checkout (no node_modules): install 23 s, Jest 38/38 (5 suites), tsc clean, lint 0 errors (`test_reports/clean_checkout_2026-09-12.txt`).
- Frontend tests: `src/__tests__/reviewAccessScreen.test.tsx` (login link, gating, 4 role destinations, 503/429/401, masking, back), `src/__tests__/appConfig.test.ts` (release origin resolution).
- Backend full shared suite: 97 passed, 5 skipped (`test_reports/pytest/release_pass_2026-09-12.xml`). Testing agent iteration 23 (live preview + backend): all pass — four reviewer roles reach their destinations with the banner; health semantics verified (`test_reports/iteration_23.json`).
- PREVIEW SMS TEST executed 14:17 UTC: dispatch accepted (sms_log `91fda9b6-…`), MSG91 report Delivered 19:47:25 IST; OTP verification/role routing NOT performed by the agent (preview record is customer). Not production evidence.
- Preview reviewer keys rotated before and after the testing-agent run; current notes only in `/tmp/yash-private/preview-<reviewer_id>.txt` of this container.
- `WEBSITE_RELEASE_HANDOFF.zip` built by `backend/tools/build_release_handoff.py` (secret scan, per-file sha256, schema checksum, build/Expo identity, baseline `6a6cddd` vs local implementation `2daa3ff` vs final source-tree digest, status: deployment pending / login unverified).
- Owner next: Save to GitHub → note final SHA → first republish → Secrets (`STAFF_SERVICE_KEY`, `REVIEW_DB_NAME`, `BUILD_COMMIT`, frontend `EXPO_PUBLIC_BACKEND_URL`) → second republish → recovery dry-run/apply/confirm → `Provision-ReviewAccess.ps1 -Environment production … -Verify` → production OTP test with role check on app + both website domains → device checks. Website agent: declare its `.env` names (incl. both origins) per `WEBSITE_AUTH_FIX_PROMPT.md` §0.

## Scope of this iteration (user request)
1. Android Expo Go Ionicons failure (empty font / EAS CDN 403): centralized runtime gate, bounded single-flight fallback to a backend-served fixed asset, no OTP/production impact. 2. Website dependencies D2/D3/D4. 3. Store-review access: isolated reviewer accounts (customer/admin/telecaller/billing), separate review database, strong reusable hashed secrets, synthetic data, genuine but bounded AI; no OTP bypass for real users, no hardcoded universal secret; owner-run provisioning with private .txt note. 4. Production recovery handoff rewritten for the OWNER as operator (Emergent Manage Publishes → Secrets/Database, Redeploy) with a Windows procedure; preserve safeguards; target only 9999813334 / bcdf18c9-dc87-4d46-b580-30cf519103df. 5. Real OTP tests to 9999813334 are authorised (one SMS + max one resend, owner-executed after production config + recovery). 6. Website handoff/release evidence with commit, contract, Expo identity, storage-deletion capability.

## Implemented and verified locally
- Frontend `src/fonts/`: `IconFontGate.tsx` (root gate in `app/_layout.tsx`, system-font loading/error states, TRY AGAIN, splash deadline 15 s), `iconFontLoader.ts` (12 s per-stage timeout, 1 auto retry, single-flight, manual retry re-runs loaders), `fallbackFont.ts` (backend `GET /api/fonts/ionicons.ttf`, validation of status/content-type/389 724 bytes/SHA-256, native cache file replacement only, web Blob URL). Jest 25/25 (RNTL v14 async APIs; deps `test-renderer`, `@types/jest` added via expo install); `tsc --noEmit` clean; ESLint clean. `test_reports/jest_icon_font_2026-09-12.txt`.
- Backend `shared/fonts.py` serves the exact version-matched Ionicons.ttf (+manifest, MIT licence) with GET/HEAD split (no duplicate OpenAPI operation id).
- Store review: `shared/review.py` (`POST /api/auth/review/login`, bcrypt, constant-cost unknown IDs, per-IP/per-account rate limits, audit rows in review DB), `shared/review_seed.py` (synthetic dataset, provision/rotate/revoke/status), `core` scope gates (db/SMS/media; workers iterate scopes), review AI bound in `server.py::ai_chat` (600 chars/message, 40 msgs/day). CLI `tools/provision_review_access.py` gained `--environment preview|production`, `--api-base-url`, `--verify` (login→/auth/me role+scope→logout per issued key, no secrets in output), `--write-note` (private file outside repo, refused before any write if inside repo/exists; 0600). Windows wrappers `tools/windows/Setup-Operator.ps1`, `Provision-ReviewAccess.ps1`, `Recover-OwnerAdmin.ps1` (hidden connection-string prompt, process-scoped env, fixed recovery target); `tools/requirements-operator.txt` proven in a fresh venv (both commands ran; recovery dry-run on preview DB returned a report with no writes).
- D2 `GET /customers/search` (before `/customers/{uid}`), D3 `GET /requests?customer_id=` (exact, 422/404 codes), D4 consumer-scoped outbox + idempotent ack; `GET/PATCH /customers/{uid}` now 404 `CUSTOMER_NOT_FOUND` for unknown canonical IDs (was phone-parse 422; found by testing agent iteration 22).
- Config: `backend/.env` preview gained `REVIEW_DB_NAME=jewellers_app_review` (working preview review DB) and `STAFF_SERVICE_KEY=SET_IN_PUBLISH_SECRETS` (deliberately <32 chars → staff flow fails closed; health `flows.staff.ready=false` expected in preview). `.env.example` updated. `APP_BUILD` aligned with `core.BUILD`.
- Environment fix: MongoDB in the preview container crashed repeatedly with "Too many open files" (soft limit 1024) during test runs → supervisor command now `ulimit -n 65536 && exec mongod`; leftover test DBs dropped.
- Tests: full shared backend suite **93 passed, 5 skipped (Playwright unavailable), 0 failed** (`test_reports/pytest/review_fonts_2026-09-12.xml`); new `test_review_provisioning_cli.py` (6: subprocess CLI provision/idempotent repeat/status, fail-closed config, private note + rotate/revoke/reset, verify step, bounded AI, background-job scope). Testing agent iteration 22 (live preview, frontend + backend): 22/22 backend checks, frontend gate/login/review-access/customer+admin reviewer sessions with banner all pass (`test_reports/iteration_22.json`).
- Preview reviewer accounts provisioned + verified against the deployed preview backend (all four roles PASS), keys only in `/tmp/yash-private/preview-review-access.txt` inside the container, rotated after the testing-agent run (`test_reports/review_provisioning_preview_2026-09-12.json`). Not store credentials.
- Docs: `PRODUCTION_ADMIN_RECOVERY.md` (owner runbook: setup, exact setting names incl. STAFF_SERVICE_KEY/REVIEW_DB_NAME, backup, dry-run/apply/confirm, review accounts, real-OTP test steps, no-SMS checks, safeguards), new `STORE_REVIEW_ACCESS.md`, `WEBSITE_HANDOFF.md` (D2/D3/D4 contract table, commit provenance, operator model), `YASH_SHARED_API_CONTRACT.md`, `WEBSITE_AUTH_FIX_PROMPT.md`, `RELEASE_READINESS.md` (build table, storage-provider finding, Expo identity, stage matrix); `contracts/openapi.shared-v1.json` regenerated (123 paths).

## Findings to carry forward
- Media provider = Emergent Managed Object Storage: **no DELETE/rename/list API** (confirmed via integration playbook). Deletion = access revocation + tombstone; byte erasure requires S3/R2/GCS migration — owner decision.
- Expo: no EAS project ID / updates URL / runtimeVersion in `app.json`; preview QR is the Metro dev server; store builds come from Emergent Publish after the implementation commit. HEAD `6a6cddd` is the pre-change pin; implementation commit is created by Save to GitHub.
- Native (Android Expo Go) icon-font behaviour is NOT verified here; device cache corruption is a tested hypothesis, not demonstrated.

## Owner next actions (in order)
1. Save to GitHub → record implementation SHA. 2. Manage Publishes → Secrets: set `STAFF_SERVICE_KEY` (≥32, ≠ enrollment key; same on website), `REVIEW_DB_NAME` (≠ DB_NAME), `BUILD_COMMIT`; Redeploy (not fresh DB); check `/api/health`. 3. Windows: `Setup-Operator.ps1`; backup; `Recover-OwnerAdmin.ps1` dry-run → apply → confirm. 4. `Provision-ReviewAccess.ps1 -Environment production … -Verify -NotePath <private>`; paste note text into App Store Connect / Play Console. 5. Real OTP login test on app + both website domains as 9999813334 (one SMS, ≤1 resend). 6. Physical Android/iOS build checks (icon font on the failing device, reviewer sign-in). Backlog: storage migration decision; website adopts the new commit (XFAILs D2/D3/D4 → pass).

# Previous phase — owner login recovery, 12 September 2026 (superseded operator model; facts retained)

## Current user request and scope
User reports website staff-login unavailable after demo OTP removal and explicitly authorises promoting existing app phone9999813334 CUSTOMER to ADMIN in production; preserve canonical ID/history and supply a website handoff prompt. No role bypass, duplicate admin, fixed OTP, or bulk identity merge. Website settings are a distinct pre-role blocker.

## Current architecture / implementation
Existing ExpoSDK54 + FastAPI + Mongo + MSG91 remain unchanged. New backend/shared/admin_recovery.py and tools/recover_owner_admin.py are OPERATOR-ONLY, never HTTP/startup imported. Dry-run no writes, exact database/ID/phone, unique-identity and active/deleted guards, snapshot hash, backup/maintenance/operator approval, intent audit then atomic same-user role+sv+event update. Recovery bookkeeping only revokes families referenced by old-version refresh tokens; completed or partial replay does not touch fresh sessions. No production repair executed.
New shared/readiness.py replaces misleading health status with per-flow mobile/staff/enrollment/deletion config+DB readiness; /health/live separate. Protected non-phone/no-SMS staff/enrollment readiness verifies key matching. Trimmed equal staff/enrollment credentials now fail consistently. OpenAPI120paths regenerated. frontend/app.config.js uses EXPO_DEVTOOLS_ORIGIN only with NODE_ENV=development for supported Expo Router preview-origin allowlisting; production native origin untouched.

## Live evidence / remaining P0
- Fresh production GET12September: app shared-v1-followup-2026-09-11, STAFF_SERVICE_KEY absent. Both website domains website-shared-v1-auth-readiness-v1 missing CANONICAL_API_BASE_URL/ENROLLMENT_INTEGRATION_KEY/STAFF_SERVICE_KEY. New live endpoint404 until new code activated.
- Authenticated production READ ONLY customer lookup confirms9999813334 active/verified/CUSTOMER, idbcdf18c9-dc87-4d46-b580-30cf519103df. Current local app database empty; no production settings writer/Mongo connection/admin session. No OTP dispatched or account changed.
- User approval now covers ONLY this same-ID role repair, NOT all identity merges. Full website export does not block this scoped recovery. Need authorised live operator to review dry-run/backup and apply, privately configure matching distinct keys on both projects, and verify both website origin allowlists. See PRODUCTION_ADMIN_RECOVERY.md; copy/paste WEBSITE_AUTH_FIX_PROMPT.md.
- Iteration21 final testing-agent verification:19/19 focused recovery/readiness tests,29/29 shared regressions (additional12/12 auth/people run overlaps), preview smartphone smoke and allowed/rejected embedded origins pass. Partial replay protects fresh access+refresh; completed replay idempotent. No local implementation issues remain; production /health/live404 is an unresolved gate. Fixture SMS/storage intercepted; no production login success claimed. Removed duplicate EXPO_PUBLIC_ENROLLMENT_URL; canonical register.yashsilver.com is now the sole configured registration URL.

## Backlog priorities
P0: authorised live configuration+single-account role repair and genuine owner OTP acceptance on app and both domains. P1: complete separately approved full identity mapping/cutover, storage plan and original release gates. P2: physical native camera/notifications/sharing acceptance. Historical follow-up below remains otherwise preserved.

# Previous phase record — 11 September2026

## Problem / constraints
Finish shared backend integration and prepare for10000photos without redoing architecture, duplicating database/media authority, losing genuine records, exposing secrets or premature rollout. Preserve owner9999813334 canonical ID/history. No9711881372 password-admin creation, no universal OTP, no production merge without specific report-hash approval/restored backup. Website private export must not block independent app work.

## Architecture and implementation
Expo SDK54 React Native app + canonical FastAPI/Motor Mongo; existing Emergent managed binary storage. Shared core/auth/people/queries/commerce/catalog/units/pdf_jobs/pdf_authoring/media_lifecycle/media_cache modules replace relevant legacy routes; remaining server/panel functionality preserved.
- Baseline OTP/grants/revocable15minute access/30day refresh families, same-subject staff exchange, role/last-admin guards, local deletion/tombstones/outbox preserved. STAFF_SERVICE_KEY remains absent and now must also differ from enrollment credential.
- Legacy grams/per-pair normalize only on explicit valid writes; unchanged historical optional fields do not block title/photo updates. Structured INR labour amount/basis with old labelled adapter; ambiguity flagged, no implicit scaling; old slab controls versions/display corrected.
- Catalog limit<=100, stable created_at/id order from page1, indexed words/exact SKU, separate discovery. Admin product-catalog FlatList40/page; thumbnail-first/private images. New catalog-author form accepts photos/fields, saves hidden product or exports exact v1 PDF. Same generator/schema/sample; no second PDF layout.
- Owner-only upright source page geometry/PNG, labelled review, actual drag/resize crop. Fixed GET-preview mutation-lock contention and new-screen back stack duplication. Source assembly independently locked.
- Managed-write intent/bytes/hash ledger, unknown outcomes, budgets/high-watermarks, media-usage UI; candidate audit protects referenced/shared/source media. Provider DELETE unavailable; remote_deleted never true. Byte-bounded16MiB/60second internal public-byte cache with authorization before EVERY fetch and HTTP private,no-store.
- Customer date/assignment controls, named telecaller/assignee/resolver options, daily/range drilldowns. Canonical analytics/dashboard retained. No admin-targeted customer phone change; self-service subject only.

## Verification / evidence
54 shared tests passed together,5 routed mobile-web journeys; final JUnit test_reports/pytest/followup_combined_after_races.xml. Isolated SMS/storage doubles; browser File multipart capture fixes test bridge omission, not production bypass. Actual crop move/resize and rotatedPDF tests pass. Added controlled PDFchunk/commit/cancel races, master-success/thumb-fail recovery and simulated expired-lease checkpoint resume. Six real read-only managed-storage reads total608598bytes; no livewrite/SMS/deletion/rotation.
10000synthetic metadata rows/100pages/unique IDs,100rowpayload62040bytes; exacttimings test_reports/catalog_10k_benchmark_iteration16.json. Browser scrolling metrics test_reports/mobile_web_catalog_scroll_metrics_iteration16.json (not native RAM). Sample3products/noerrors1024master320thumb, MAE2.439/1.6606/1.2307. Current60product/32page4,128,423byte subprocess fixture54.641s; parent+child235,646,976byte peak/concurrency1 excludes fullserviceworkload. TypeScript/lint/compile checked. Physical devices/provider failure chaos unavailable.

## P0 release gates
1. Save to GitHub is user-controlled/unavailable to agent; branchmain local baseline30796997d3484594c6c5f53965e1c71dd5ed1c86 publicly verified beforeediting, NOT newimplementationcommit. Follow-upSHA/pinnedlinks pending action. No productiondeploy; previewbuildshared-v1-followup-2026-09-11 commitunrecorded; production independent lastv7 commitunknown.
2. Authorized production+website secrets, separate staffkey/rotation, genuine MSG91dispatch+receipt.
3. Account-specific storage/DB/bandwidth/readwrite/CPU/RAM/backup costs unknown; supportedDELETE or owner-approved single-store plan required. No purchase/migration authorized. Complete production inventory/controlledproviderwrite+readback/recovery testpending.
4. Minimal private identityexport, verifiedrestorablebackup, conflict/hashmappingapproval; later websiteBFF/UIstaging, coordinatedcutover/reauthentication. No actualreconciliation.
5. Privatelyprovisioned isolated Playreview roles/data; externaldeletion/privacyretention verification; physicalAndroid/iOSfiles/sharing/background/navigation; fullservice/resourcechaos tests.

## P1/P2
P1: finish genuine-provider/device/full-serviceacceptance when inputs available; durableauthoringdrafts if needed. Cleanup dormant legacy panel sections without breaking preserved flows. P2: optional optimizedservingvariants after detailreview, stablelogicalmedia-ID migration onlyifapproved, furthercursor/snapshotpaging if livecatalogeditvolume requires it. No recommendation to expand paidresources without actualentitlement/inventory.

## Handoff
All five rootdocs updated: YASH_SHARED_API_CONTRACT.md, WEBSITE_HANDOFF.md, IDENTITY_EXPORT_CONTRACT.md, PDF_VALIDATION_EVIDENCE.md, RELEASE_READINESS.md. Additional STORAGE_CAPACITY.md. Regenerated contracts/openapi.shared-v1.json and sample.pdf/sample.manifest.json. WEBSITE_HANDOFF includes endpoint/body/header/response maps, nonsecretconfigurationnames, minimalread-onlyexportinstructions and stagebothclients-beforelivecutover sequence. No privateexports/backups/reviewcredentials inGit.