# Copy/paste this prompt into the Yash Trade Website chat

Fix the production staff-login outage on BOTH `register.yashsilver.com` and `yash-register.emergent.host`, coordinating against the canonical Yash Trade App. Do not replace canonical authentication, create website-local admin authority, or re-enable demo OTPs.

App-side fresh read-only checks on 12 September 2026 confirmed:
- Production canonical base is `https://yash-tryon-test.emergent.host/api`, running `shared-v1-followup-2026-09-11` at time of observation.
- Existing canonical owner account `9999813334` is active, verified and still CUSTOMER; ID `bcdf18c9-dc87-4d46-b580-30cf519103df`.
- App production `STAFF_SERVICE_KEY` is absent. Both website domains report missing `CANONICAL_API_BASE_URL`, `ENROLLMENT_INTEGRATION_KEY`, `STAFF_SERVICE_KEY`.
- User explicitly authorised conversion of THAT existing account to admin. **Decision of 12 September 2026: 9999813334 is the DEFAULT ADMINISTRATOR in the canonical backend.** The app backend now applies it itself at every start (`OWNER_ADMIN_PHONE=9999813334`, `backend/shared/owner_admin.py`): the existing record is promoted to admin on the same canonical ID with history preserved and old sessions revoked (or created as admin where no record exists); `/api/health` reports it as `flows.owner_admin` (`state` created/promoted/already_admin). It is applied by the deployment itself — not an endpoint, not a login bypass, not a fixed OTP; sign-in stays the canonical MSG91 OTP flow. The earlier operator-only `recover_owner_admin.py` remains only as a manual alternative. **The website must NOT create a website-local admin for this number**: it reads the role from canonical `/auth/me` after the staff OTP exchange, exactly as for every other staff account, so the default admin works on the website as soon as the website's settings below are in place.

Required website work:
0. **Declare the missing settings in the website's ACTUAL backend `.env` first** (the website agent does this — not the owner): `CANONICAL_API_BASE_URL=https://yash-tryon-test.emergent.host/api`, `ENROLLMENT_INTEGRATION_KEY=SET_IN_PUBLISH_SECRETS`, `STAFF_SERVICE_KEY=SET_IN_PUBLISH_SECRETS`, and `BFF_ALLOWED_ORIGINS=https://register.yashsilver.com,https://yash-register.emergent.host` (BOTH public origins, exact, no wildcard). Preserve every existing working value; use non-working placeholders only for the two secrets. Treat any placeholder as ABSENT in executable code (fail closed, report the name as missing). Then follow the support-confirmed sequence: agent declares names in `.env` → **first republish** registers them in Manage Publishes → Secrets → the owner enters the real values privately in the existing-key editor (the owner never creates keys by hand) → **second republish**. Keys that already exist in production keep their stored Secrets value, so existing production values must be updated in the editor, not by editing `.env`. A `false` in a readiness response means that setting is missing/invalid on the website; it does not mean the canonical backend is unreachable. Report names/status only.
1. Through AUTHORIZED PRIVATE runtime settings, set `CANONICAL_API_BASE_URL=https://yash-tryon-test.emergent.host/api`. Privately migrate the matching existing `LIVE_INTEGRATION_KEY` to `ENROLLMENT_INTEGRATION_KEY` after checking its match; do not add a fallback secret.
2. Coordinate ONE separately generated `STAFF_SERVICE_KEY`, matching on the app and website, different from enrollment. A local `.env` edit is NOT proof of production configuration. Never expose keys in chat/source/client bundles; do not share canonical JWT/SMS/database credentials with the website.
3. Explicitly allow BOTH public website origins in `BFF_ALLOWED_ORIGINS`, retaining only verified aliases in ingress configuration. Do not use wildcard origins or weaken CSRF/cookies.
4. Staff BFF requests must send server-only `X-Staff-Service-Key`, with `{phone,purpose:"login",channel:"portal"}` to `/auth/send-otp` and the same fields plus `otp,challenge_id` to `/auth/verify-otp`. One canonical SMS/verification. Route using canonical `/auth/me`; customer cannot access admin. Enrollment/deletion use only `X-Integration-Key` and their purpose-bound grants. Service keys cannot assign a role or mint/impersonate a staff session.
5. Preserve the website's existing accurate 503 unavailable states. App prepared the next build `shared-v1-owner-recovery-2026-09-12` with `GET /health/live`, `/health/ready`, and credential-authenticated NON-PHONE `GET /integrations/staff/readiness` / `/integrations/enrollment/readiness`. All paths are relative to the base ending `/api`. Check `capabilities.credential_readiness=1` before using them. Send the corresponding server-only header; validate `credential_verified=true` and ready status. They send no SMS and mutate no identity. Wrong matching key returns 401; missing/weak/shared key or unavailable dependency returns 503. Public health booleans alone do not establish key matching. Do not disable the mobile flow because only staff configuration is missing. Liveness monitoring belongs on `/health/live`.
6. The **owner** is the production operator for the app (Emergent: Manage Publishes → Secrets for settings, → Database for data, Redeploy to activate). The admin role of 9999813334 is applied automatically by the app backend on its first start after the new build is deployed (`flows.owner_admin.state=promoted`, then `already_admin`); the manual `backend/tools/windows/Recover-OwnerAdmin.ps1` or the canonical conversion endpoint are alternatives only. Do not change only the website record or create another owner. **Keys do not grant admin status**: a matching `STAFF_SERVICE_KEY` only lets the BFF reach the staff OTP route; the account's role comes solely from the canonical record. Once both projects' settings are complete and the app build is live, the owner performs one genuine OTP sign-in per surface (authorised for 9999813334 only: one SMS, at most one resend after the cooldown); confirm `/auth/me` is the same ID with role admin. Report dispatch, receipt, verification and role routing separately; readiness booleans never substitute for any of them. Verify both website domains, app admin access, customer denial, telecaller/billing roles, refresh and secure BFF sessions. The website must route `role=admin` from canonical `/auth/me` to its admin experience and must not offer the customer area to it.
7. Adopt the app implementation commit that follows `6a6cddd` (the owner's Save to GitHub produces it) as the new app pin: it resolves the website's strict XFAIL cases D2 (`GET /customers/search`), D3 (`GET /requests?customer_id=`) and D4 (consumer-scoped `GET /integrations/deletions` + idempotent ack). Contract details are in the app's `WEBSITE_HANDOFF.md` and `contracts/openapi.shared-v1.json`. The pre-change pin `6a6cddd` is NOT the implementation; `HANDOFF_MANIFEST.json` inside `WEBSITE_RELEASE_HANDOFF.zip` records the baseline, the local implementation commit and the final source-tree digest separately.

Use your testing agent. Return a small evidence table for each production domain and flow, distinguishing actual production observations from isolated test results. Do not send real OTPs to anyone other than the owner's 9999813334, and only after the owner confirms production is configured. If your environment cannot write the website's production settings, state exactly which names are missing so the owner can set them in that project's secret management — do not claim another preview code change restored production. Share only status/receipt names, NEVER private values. The app chat cannot edit this separate website repository or synchronize private settings by exchanging public prompts.