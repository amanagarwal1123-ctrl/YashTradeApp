/**
 * Placeholder shown by expo-image while a photo loads: a flat blurhash in the app's surface colour (#141422), so list
 * cards never flash white or stay blank. Blurhash decodes locally (no request) on iOS, Android and web.
 */
export const IMAGE_PLACEHOLDER = { blurhash: '002P0[' };
