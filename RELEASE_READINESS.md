# Release readiness — follow-up NOT production-ready

## Google Play submission changes — 14 September 2026 (current source tree)

| Item | Status | Evidence |
|---|---|---|
| Reviewer entry point moved | Login screen shows only **Help** (`login-help-link`, no session needed); `/help` lists *App review access* → existing `/review-access` screen, accounts (`review__review_accounts`) and `POST /api/auth/review/login` unchanged. Generated sign-in steps, store-form text and the private note (`review_seed.SIGN_IN_STEPS/STORE_FORM_TEXT/note_lines`) use the new path | Jest `reviewAccessScreen.test.tsx` (2 new cases), pytest `test_review_provisioning_cli.py` (note text), testing agent (this iteration) |
| Splash / launcher assets | `assets/images/splash-icon.png` was **missing** (build would fail at prebuild) and `icon.png`/`adaptive-icon.png`/`favicon.png` were the Emergent template "e" logo. All four are now generated from the owner-supplied `Yash_App_Icon_Option_1_1024px_under_1MB.png` (gold Y + diamond on green): `icon.png` 1024², `adaptive-icon.png` 1024² foreground (emblem inside the 66 % safe zone, background `#013625`), `icon-android-legacy.png` (transparent corners, API < 26), `splash-icon.png` 1024² RGBA on `#000`, `favicon.png` 256². Unused template PNGs removed. `expo prebuild --platform android` on a scratch copy succeeded and produced `splashscreen_logo`, `ic_launcher*`, `iconBackground=#013625` | `/tmp/prebuild-check` run 14 Sep (not committed); `npx expo config` asset paths all exist |
| AI-consent wording | No longer claims a "pseudonymous session identifier" is sent (it is not transmitted) or that names/phones are "never sent". States: typed text is transferred **as written** (may contain names/phones), last 10 turns + fixed instruction + gateway credential; profile fields **not attached automatically**; no provider retention period claimed. `AI_CONSENT_VERSION=2026-09-14` → existing consents are re-asked | `shared/ai_consent.py`, `AiConsentCard.tsx`, `ai-consent.tsx`; pytest `test_ai_consent_and_deletion.py` |
| Deletion: two outcomes, never merged | `required_acknowledgements` was `[website, sms_provider, ai_provider]` — the last two can never acknowledge, so every deletion stayed pending forever. Now `["website"]`; website ack → `deletion_requests.status=cleanup_completed` (**app + website cleanup only**). Provider-held copies live in the provider-erasure ledger (`shared/provider_erasure.py`: `not_applicable / not_requested / requested / no_procedure / confirmed / refused`, request date, reference, outcome, retention exception, history); `not_requested` is OUTSTANDING, never a completed deletion; historical requests back-filled as outstanding. Admin panel (Panel → **Deletions** tab → Account deletion requests) shows both outcomes and records manual provider requests (`GET/POST /api/admin/deletion-requests…`). Manual procedures per provider documented (`GOOGLE_PLAY_DATA_SAFETY.md` §4.4). **Removing the eternal pending state is not itself compliance** | `shared/people.py`, `shared/provider_erasure.py`, `src/components/staff/DeletionRequests.tsx`; pytest `test_provider_erasure_ledger.py` (4, incl. legacy-row reconciliation), Jest `deletionRequestsLedger.test.tsx` (4); live E2E testing-agent iteration 30 (backend 19/19 + full panel flow) `test_reports/iteration_30.json` |
| SMS log retention bound | `sms_log` (phone + delivery status) had no retention; TTL index on `expires_at`, 90 days (`core.SMS_LOG_RETENTION_DAYS`), back-filled for existing rows in both scopes | `shared/core.py::indexes`; preview DB: 0 rows without `expires_at` |
| Data safety answers | `GOOGLE_PLAY_DATA_SAFETY.md` — derived from code + SDK inventory, customer and staff flows, photos/PDFs, collection vs sharing per Google's definitions, retention table, provider limitations, owner checklist | file |
| Website privacy wording | `WEBSITE_PRIVACY_UPDATE.md` — exact replacement text (AI recipients, withdrawal, staff uploads, deletion/retention, review__ storage note); added to `build_release_handoff.py` DOCUMENTS | file |
| Still blocking submission (owner) | Publish the updated privacy policy and point `EXPO_PUBLIC_PRIVACY_URL` at it; confirm the website *Delete account* URL; confirm platform log/backup retention for the two `[OWNER: …]` placeholders; Save to GitHub → republish → real owner OTP → Panel > Store review → PROVISION → paste note; upload 512² store icon; object-storage byte erasure remains impossible with the current provider (disclosed) | — |

## Store-submission build — 13 September 2026 (`shared-v1-store-submission-2026-09-13`, current source tree)

Supersedes the review-storage model of the 12 Sep rows below: **there is no separate review database and no `REVIEW_DB_NAME` any more.** Store-review isolation = `review__*` prefixed collections inside the same `DB_NAME`, switched on by `REVIEW_ACCESS_ENABLED=true` (declared with its real value in `backend/.env`), enforced by the signature-verified session scope. Rows in the older sections that mention `REVIEW_DB_NAME`, `REVIEW_DB_USABLE`, `REVIEW_DB_UNAUTHORIZED` or a "separate/isolated review database" are historical records of that day and no longer describe the code.

| Item | Status | Evidence |
|---|---|---|
| Store-review isolation (prefixed collections) | `core._PrefixedDatabase` resolves every collection touched in review scope to `review__<name>`; production scope never resolves a prefixed name; `$lookup` targets go through `core.collection_name()`; media in `review__review_blobs` (5 MiB/file, 256 MiB); SMS simulated; workers iterate production then review in separate scopes | pytest `test_review_access_isolation.py` 9/9, `test_review_prefixed_storage.py` 2/2 (real `mongod --auth`, user restricted to the main database), live preview iterations 26/27 (unprefixed counts unchanged) |
| Owner console `/review-keys` (`GET/POST /api/admin/review/{status,challenge,keys}`) | Owner administrator only (server-side: `403 REVIEW_SCOPE_FORBIDDEN` for reviewer admins, `403 OWNER_ADMIN_REQUIRED` for other admins); provision / rotate / revoke / reset_data each need a fresh single-use owner OTP; keys shown once (copy / private note / .txt), bcrypt hashes stored | pytest `test_review_owner_console.py` 4/4; Jest `reviewKeysAndAiConsent.test.tsx`, `reviewKeysAccessGate.test.tsx` (owner, reviewer admin, other admin, signed-out, hydrating); live preview 13 Sep: reviewer admin and disposable non-owner admin → locked cards, signed-out → `/login` |
| **Fix 13 Sep — web session lost on navigator reset** | On web the AuthProvider remounts when React Navigation resets its history (e.g. `popstate` to a URL outside its stack); the memory-only session was dropped and screens redirected to `/login` (iteration 27 residual). `AuthContext` now re-hydrates from the in-memory module token and re-validates it with `GET /auth/me` — still memory-only (a page reload starts signed out), still server-decided (a revoked session ends in signed-out), no client-side role decision | Jest `authSessionWeb.test.tsx` 5/5; live preview browser run (reviewer admin, non-owner admin, signed-out; client-side navigation and hard reload) |
| AI consent (`GET/POST /api/ai/consent`, `403 AI_CONSENT_REQUIRED` on `/api/ai/chat`) | No text reaches the provider without current consent; withdrawal deletes stored AI chat history and an in-flight reply after withdrawal is not stored; consent card in the assistant and in Profile → AI Data Sharing | pytest `test_ai_consent_and_deletion.py` 3/3; live preview iteration 27 F3 (real provider reply once) |
| Deep account deletion | Removes analytics, AI history, consent, reports; `session_version` advances → old bearer `401 SESSION_REVOKED`; tombstone + outbox event; reviewer sample profile is recreated fresh on the next sign-in with an ENABLED key | pytest (`test_ai_consent_and_deletion.py`, isolation suite); live preview iteration 27 B6/B7/F4 |
| `.env` declarations | `REVIEW_ACCESS_ENABLED=true`, `OWNER_ADMIN_PHONE=9999813334` (real values); `STAFF_SERVICE_KEY`, `BUILD_COMMIT` placeholders; `REVIEW_DB_NAME` removed | preview `/api/health`: `flows.review {ready:true, storage:prefixed_collections, accounts_enabled:0}`, `flows.owner_admin.state=already_admin`, `configuration.REVIEW_ACCESS_ENABLED=true`, `REVIEW_STORAGE_USABLE=true`, `STAFF_SERVICE_KEY=false`, `commit:"unrecorded"` |
| Preview reviewer accounts | Provisioned for automated runs only, **all four REVOKED** afterwards (`accounts_enabled=0`); keys never in the repository or chat | CLI `--status` |
| Not verified here | Production provisioning, production reviewer sign-ins, owner console positive path with a real OTP, physical devices, managed-object-storage byte erasure (provider has no DELETE API — deletion = access revocation + tombstone) | owner steps in `STORE_REVIEW_ACCESS.md` / `PRODUCTION_ADMIN_RECOVERY.md` |

## Release pass — 12 September 2026 (final packaging of `shared-v1-review-fonts-2026-09-12`)

Settled owner decisions for this pass: **Yarn** is the only package manager (lockfile `frontend/yarn.lock`; `package-lock.json` removed); `frontend/app/review-access.tsx` is tracked; `WEBSITE_RELEASE_HANDOFF.zip` is the deliverable; one **preview** SMS to 9999813334 was authorised and sent; the agent owns the `.env` name declarations. Recorded in `memory/PRD.md` so they are not reopened.

| Item | Status | Evidence |
|---|---|---|
| **Default owner administrator — 9999813334 on app AND website (owner decision 12 Sep, after the hotfix)** | `OWNER_ADMIN_PHONE=9999813334` declared in `backend/.env` (real value; carried into every deployment). `backend/shared/owner_admin.py` runs at startup right after the primary indexes: the single record with that phone is promoted to admin **on the same canonical record** (history kept, one audit event `owner_admin_bootstrap`, `session_version+1`, open session families revoked once) or **created** as an active admin when missing (no password, no OTP, no token); an active admin is left untouched (sessions kept). Refuses without any change: two records for the phone (`OWNER_ADMIN_IDENTITY_CONFLICT`), inactive/deleted record (`OWNER_ADMIN_ACCOUNT_INACTIVE`), tombstoned identity (`OWNER_ADMIN_DELETED_IDENTITY`), missing/placeholder/invalid phone (`OWNER_ADMIN_PHONE`), DB/config error (`OWNER_ADMIN_NOT_APPLIED`); never in the review scope. `/api/health` `flows.owner_admin {ready, issues, state, phone_suffix, detail}` (non-optional → affects `ready`), `configuration.OWNER_ADMIN_PHONE`, capability `owner_admin_bootstrap`. Staff API cannot demote/disable/delete the owner (`409 OWNER_ADMIN_PROTECTED`). Sign-in unchanged: MSG91 OTP on the app (`channel=mobile`) and the website's staff exchange (`channel=portal` + `STAFF_SERVICE_KEY`) both return `role=admin` from that record. Manual `Recover-OwnerAdmin.ps1` kept as an alternative only | `test_owner_admin_bootstrap.py` **5/5**: creation + idempotent restart + admin sign-in on both channels; same-ID promotion of a seeded customer (history/rewards kept, old access + refresh tokens 401, other account untouched); refusals with unchanged data and truthful health; protection (PATCH role/status, DELETE → 409; rename OK; self-deletion 403; `erase()` refused); real `server.startup()` path. Hotfix suite: the restricted-user backend process creates the owner admin in the main DB (`flows.owner_admin.state=created`). Full shared suite 109 passed / 5 skipped (`test_reports/pytest/owner_admin_full_2026-09-12.xml`) |
| **Production hotfix — review storage authorisation (12 Sep, after first deployment attempt)** | The deployed backend crashed at startup because its MongoDB user is authorised for the main database only and the configured review database (`jewellers_app_review`) rejected index creation (MongoDB code 13). Now: `core.initialize_review()` proves the optional review database (ping + indexes) or marks it **unusable for this process** — startup continues, `/api/health` reports `flows.review.ready=false`, `issues=[REVIEW_DB_UNAUTHORIZED]` (or `REVIEW_DB_UNAVAILABLE`), `configured=true`, `usable=false`, `configuration.REVIEW_DB_USABLE=false`; reviewer sign-in **and** existing reviewer sessions/refresh tokens answer 503 `REVIEW_UNAVAILABLE`; background workers iterate only usable scopes (the unauthorised database is never polled again); the review database is never created and no review artefact lands in the main database; the log carries one sanitized warning (no connection string). A failing **primary** database still aborts startup; `REVIEW_DB_NAME == DB_NAME` still refuses to start. Preview `backend/.env` now declares `REVIEW_DB_NAME=SET_IN_PUBLISH_SECRETS` (no review database anywhere until the owner sets a real, distinct, authorised name in Secrets) | `test_review_storage_hotfix.py` **7/7**: real `mongod --auth` instance with a user holding readWrite on the main test database only → backend process starts, health/503/worker-idle/log assertions; injected code-13 failure → scopes exclude review, workers never touch the handle; separate authorised review database still initialises; primary failure not absorbed; placeholder never produces a handle. Full shared suite: `test_reports/pytest/hotfix_full_2026-09-12.xml` |
| Reviewer provisioning CLI strictness | `outcome` (run result) separated from `status` (account table); logout must return `logged_out=true` **and** the old token must be rejected (401) or the account is FAIL; keys are written to the private note **before** network verification; `--verify-note` read-only recovery (no DB) with a strict **pre-flight before any request**: the note's header must name the same environment, review database **and backend URL** (scheme, host, port and path all equal to `--api-base-url`; a note without a recorded backend is refused) → exit 1, nothing sent; a transport failure **after** a valid pre-flight → exit 2 `verification_incomplete` with a sanitized `NOT COMPLETED - <ErrorType>` entry appended to the note (no key, no payload); exit **0/1/2** contract; Windows wrapper `-VerifyNote`, exit-code messages, production issuance requires `-NotePath` | `test_review_provisioning_cli.py` **8/8** incl. `test_cli_verify_against_live_backend_returns_strict_exit_codes` (real uvicorn process: provision+verify → 0; revoked key → 2 `verification_failed`; unreachable backend → 2 `verification_incomplete` with the rotated key intact and usable; `--verify-note` same target → 0; `--verify-note` against another port → 1 with no sign-in reaching the server and the note untouched; `--verify-note` against the pinned but unreachable backend → 2 with the sanitized note entry; wrong environment / mixed flags / production without note → 1) and unit cases for host/port/scheme/path mismatches and the `(not recorded)` header |
| Placeholder configuration cannot act | `SET_IN_PUBLISH_SECRETS` for `REVIEW_DB_NAME` → review `ready=false`, reviewer login 503, **no database created**; for `STAFF_SERVICE_KEY` → 503 `CONFIGURATION_REQUIRED`; for `BUILD_COMMIT` → `commit:"unrecorded"`; CLI refuses before touching Mongo | `test_placeholder_configuration.py` **2/2** (backend started as a separate process with placeholder values) |
| `.env` declarations (names only) | `backend/.env`: `STAFF_SERVICE_KEY` (placeholder, fails closed), `REVIEW_DB_NAME` (**placeholder since the hotfix** — no review database is configured in preview or in any deployment until the owner sets a real, distinct, authorised name in Secrets; the earlier preview value `jewellers_app_review` was what the production deployment inherited and could not access), `BUILD_COMMIT` (placeholder → unrecorded); `frontend/.env`: `EXPO_PUBLIC_PRODUCTION_BACKEND_URL=https://yash-tryon-test.emergent.host`. Sequence: declare → republish → owner edits values in Secrets → republish | `/api/health` on preview: `flows.review.ready=false (REVIEW_DB_NAME)`, `flows.staff.ready=false (STAFF_SERVICE_KEY)`, `commit:"unrecorded"` — configuration checks only; reviewer sign-in on preview answers 503 until a name is set |
| Release backend origin | `frontend/app.config.js`: `NODE_ENV=production` (Publish web export, store builds) resolves `extra.backendUrl` to `https://yash-tryon-test.emergent.host`; a `*.preview.emergentagent.com` or empty value is never shipped; development preview unchanged | `NODE_ENV=production expo config --type public` → `extra.backendUrl=https://yash-tryon-test.emergent.host`; `src/__tests__/appConfig.test.ts` 5/5. **The deployed bundle itself is verified only after the owner publishes** (check `Constants.expoConfig.extra.backendUrl` / network calls in the released app) |
| GitHub packaging | Root `.gitignore` rule `review-access*` (which hid the reviewer screen) narrowed to private notes `*review-access*.txt|log|bak` + explicit `!frontend/app/review-access.tsx`; `package-lock.json` deleted; `frontend/README.md` uses Yarn. **`frontend/yarn.lock` was missing from the last GitHub save** because it had never been staged (the release-pass commit deleted `package-lock.json` but did not add the Yarn lockfile, and the platform save commits only already-tracked files); it is now tracked and committed together with the hotfix test and `backend/.env.example` | `git check-ignore`: screen not ignored, `preview-review-access.txt` still ignored; `git ls-files frontend/yarn.lock` lists the lockfile and its Git blob id is recorded in `HANDOFF_MANIFEST.json` (`tracked_lockfile`). **Clean checkout** (fresh copy without `node_modules`): `yarn install --frozen-lockfile` OK (23 s), `yarn test` **38/38** (5 suites), `tsc --noEmit` clean, `yarn lint` — 1 pre-existing error (`SmsDiagnostics.tsx` unescaped apostrophe) fixed; see `test_reports/clean_checkout_2026-09-12.txt` |
| Reviewer screen + role destinations | Jest: login link → `/review-access`; customer → `/(tabs)`, admin & billing → `/panel`, telecaller → `/telecaller`; server decides the role; 503/429/401 messages; key masking; back | `src/__tests__/reviewAccessScreen.test.tsx` 8/8; live preview: testing-agent run (see `test_reports/iteration_23.json`) |
| PREVIEW SMS test (authorised, one SMS) | `POST /api/auth/send-otp` login/mobile for 9999813334 on the **preview** backend: **dispatch** HTTP 200, MSG91 accepted (`sms_log` `91fda9b6-…`); **receipt** per MSG91 delivery report `Delivered` 19:47:25 IST (provider report, not handset confirmation); **OTP verification** not performed by the agent; **role routing** not verified (preview record is `customer`). Not production evidence | `PRODUCTION_ADMIN_RECOVERY.md` → Status |
| Handoff package | `WEBSITE_RELEASE_HANDOFF.zip` (repo root): 7 Markdown docs + `contracts/openapi.shared-v1.json` + `HANDOFF_MANIFEST.json`. The manifest is derived from the **Git objects of the source commit** (the exact blobs GitHub shows): per file `git_blob_sha1` + SHA-256 of the blob bytes, the tracked `frontend/yarn.lock` blob, schema checksum, build/Expo identity, baseline `6a6cddd` vs `source_provenance.source_commit.sha`, a digest of the whole committed tree, deployment/test status. The builder refuses a dirty working tree, an untracked lockfile or any non-Yarn lockfile, so the package can only describe what is committed. No `.env`, credentials, OTPs, private notes or customer data | manifest inside the zip; `python backend/tools/build_release_handoff.py` |

**Still pending (owner):** Save to GitHub (final SHA), first republish (registers names), Secrets values + second republish, `Recover-OwnerAdmin.ps1` dry-run/apply/confirm, `Provision-ReviewAccess.ps1 -Environment production … -Verify`, production OTP test on app + both website domains with `/auth/me` role=admin on the SAME canonical ID, physical-device checks. **Production login is unverified.**

## Review-fonts build — 12 September 2026 (`shared-v1-review-fonts-2026-09-12`)

**What changed in this build**

| Item | Status | Evidence |
|---|---|---|
| Android Expo Go icon-font failure (Ionicons empty file / EAS CDN 403) | Root gate `IconFontGate` holds icon-bearing routes until the exact `Ionicons.font` map is registered; bounded single-flight loader (12 s timeout per stage, one automatic retry, deliberate TRY AGAIN); last-resort fallback fetches the version-matched `Ionicons.ttf` from our backend `GET /api/fonts/ionicons.ttf`, validated by status, content type, byte length **389 724** and SHA-256 before it is registered; native cache copy is the only file ever replaced; splash hard deadline 15 s. | Jest **25/25** (`frontend/src/fonts/__tests__`: initial loading gate, successful bundled load, already-loaded skip, empty/corrupt font → validated fallback, offline/both-fail error state with bounded retry, timeout classification, HTML/truncated/tampered fallback rejection, native cache replacement); pytest `test_icon_font_fallback.py` (exact bytes/hash/headers, HEAD, manifest, no path proxying); TypeScript `tsc --noEmit` clean; ESLint clean. **Native verification NOT done**: browser preview and mocked tests do not prove the Android Expo Go crash is fixed; the original device's cache corruption is a hypothesis reproduced in tests, not independently demonstrated. Verify on a physical Android device with the next build. |
| Website dependencies D2/D3/D4 | Implemented and documented (`WEBSITE_HANDOFF.md` contract table) | pytest `test_website_dependencies_d2_d3_d4.py` 9/9 |
| Store-review access | Isolated review database, four reviewer roles, hashed reusable keys, owner-run CLI + Windows wrapper with private note and deployed-backend verification, bounded genuine AI | pytest `test_review_access_isolation.py` 8/8 + `test_review_provisioning_cli.py` 6/6 (real subprocess CLI); preview end-to-end provisioning + verification of all four roles against the deployed preview backend: `test_reports/review_provisioning_preview_2026-09-12.json`. **Production accounts not provisioned** (owner action, `STORE_REVIEW_ACCESS.md`). |
| Owner production runbook | Rewritten for the owner as operator (Manage Publishes → Secrets/Database, Redeploy), Windows scripts `backend/tools/windows/*.ps1`, minimal `requirements-operator.txt` | Both operator commands executed from a fresh minimal venv; recovery dry-run against the preview DB returned a full report with no writes. **Production recovery, settings and real OTP test not yet performed** (owner steps in `PRODUCTION_ADMIN_RECOVERY.md` §1–5). |
| Full shared backend suite | `test_reports/pytest/review_fonts_2026-09-12.xml` | see totals recorded in `memory/PRD.md` (Playwright-based UI cases skip in this Python environment as before) |

**Remote media deletion — provider capability (decision input, not a blocker hidden from readiness).** The app's media provider is **Emergent Managed Object Storage** (`INTEGRATION_PROXY_URL` → `/objstore/api/v1/storage`, authenticated with `EMERGENT_LLM_KEY`). Its API offers `init`, `PUT objects/{path}`, `GET objects/{path}` only — **no DELETE, rename, list or presigned URL**. Consequently `remote_deleted` is never true, `GET /api/admin/media/usage` reports `provider_delete_supported:false`, lifecycle audits return `blocked_provider_unsupported`, and deleted/unpublished media are protected only by authorisation at every fetch (private, `no-store`) and by never re-serving unreferenced paths. Bytes remain in the provider's bucket. If store Data-Safety/privacy answers or a retention policy require **actual byte erasure**, the only path is a provider with DELETE (S3/Cloudflare R2/GCS) and a migration of new writes plus a one-off copy/verification of referenced objects — an owner decision on cost/entitlement (`STORAGE_CAPACITY.md`). Until then, release documentation must describe deletion as *access revocation + tombstone*, not erasure.

**Expo project / update identity.** `app.json`: slug `yash-trade`, name "Yash Trade App", version 1.0.0, iOS bundle / Android package `com.emergent.yashtryontest.lt5e6b`, `expo-router` + `expo-splash-screen` plugins. **No EAS project ID, `updates.url` or `runtimeVersion` is configured** — there is no EAS Update channel; the QR code shown in this workspace is the Metro development server of the preview container and it serves whatever source is currently in the workspace, not a published update. Store builds are produced by the Emergent *Publish* flow from the committed source; the build that contains these changes is the one produced **after** the implementation commit. Record for the release: implementation commit SHA (from Save to GitHub), `BUILD_COMMIT` set to it in production Secrets, and the Publish build identifier the platform returns.

**Distinguish clearly**

| Stage | State |
|---|---|
| Local completion | Done: code, tests (backend + Jest, clean-checkout Yarn install/test/tsc/lint), docs, OpenAPI regenerated, preview provisioning verified, handoff zip |
| GitHub publication | **Pending** — owner's Save to GitHub creates the final source commit; `6a6cddd` is the pre-change baseline (website pin), `2daa3ff` the local review-fonts implementation commit; neither is the final release commit |
| Production deployment | **Pending** — owner: Save to GitHub → republish → Secrets (`STAFF_SERVICE_KEY`, `BUILD_COMMIT`, frontend `EXPO_PUBLIC_BACKEND_URL`; **no `REVIEW_DB_NAME`**) → republish → health checks (configuration only; `flows.owner_admin.state=promoted` on the first start) → production OTP login as 9999813334 → reviewer provisioning from the owner console (or CLI) → four reviewer sign-ins |
| Physical-device verification | **Pending** — Android/iOS build from Publish: icon font on a previously failing device, reviewer sign-in, OTP login as admin |

## Owner-login recovery update — 12 September 2026

**Production login remains blocked; NOT restored.** Fresh GET checks (not cached crawl) show app build `shared-v1-followup-2026-09-11` missing STAFF_SERVICE_KEY; both website domains run `website-shared-v1-auth-readiness-v1` missing canonical base/enrollment/staff configuration. Production `/api/health/live` is currently404 because the new code is not running there. Existing owner9999813334 is active/verified/CUSTOMER, canonical ID confirmed in PRODUCTION_ADMIN_RECOVERY.md. No real role/settings changes or SMS occurred.

Current workspace build `shared-v1-owner-recovery-2026-09-12` adds guarded operator-only recovery, per-flow readiness/no-SMS credential checks, trimmed key separation, and a development-only embedded-preview origin configuration. Generated OpenAPI now120 paths. Full website export is NOT a prerequisite for the owner's newly authorised single-account role repair; full identity merging remains gated. Production Mongo/settings access is not available to this workspace. Exact live maintenance and website coordination instructions: PRODUCTION_ADMIN_RECOVERY.md and WEBSITE_AUTH_FIX_PROMPT.md.

Iteration20 testing found/reproduced a partial-recovery replay edge case; code now revokes only pre-repair version families, protecting fresh sessions. Iteration21 testing-agent retest passed19/19 focused tests and29/29 shared regressions; additional12/12 auth/people run overlaps that regression suite. Mobile preview and development-only allowed/rejected origin checks passed. No local implementation issue remains in that report; production probe still finds old-build /health/live404. Frontend duplicate enrollment URL removed, retaining canonical register.yashsilver.com. The baseline54 result below is historical and not a new production claim.

## Previous phase code, repository and runtime provenance

| Surface | Evidence/status |
|---|---|
| Verified pre-follow-up public main | 30796997d3484594c6c5f53965e1c71dd5ed1c86; public main checked before editing. Prior baseline-sync uncertainty is superseded. |
| New implementation | Workspace branch main. No configured git remote or agent-controlled Save to GitHub operation. New implementation SHA/commit URL/remote sync UNAVAILABLE until that user-controlled action. Never identify the baseline as this implementation. |
| Preview | https://app-first-signin.preview.emergentagent.com ; health build shared-v1-followup-2026-09-11, commit unrecorded. Source loaded in preview, exact recorded commit unknown. |
| App production | https://yash-tryon-test.emergent.host ; owner's independent11September check:2026.09.09-integration-v7; exact commit unknown. No production change performed here. |
| Website production | https://register.yashsilver.com and https://yash-register.emergent.host ; independent check2026.09.10-login-v10; commits unknown. No website access/config/code change or rollout performed. |

Code sync is not deployment. Do not stage breaking auth/session/enrollment contracts against the unprepared website. After Save to GitHub, verify the NEW branch/SHA contains all five docs/OpenAPI/artifacts and record that commit separately in staged BUILD_COMMIT. No secrets/real exports/backups/reviewer passwords belong in Git. Existing memory/test_credentials.md contains only isolated-test guidance, not real credentials.

Generated `contracts/openapi.shared-v1.json` contains116 paths; exact JSON equality with the running ingress-accessible `/api/openapi.json` was verified by curl. A final read-only public main check still returned the baseline SHA, so new remote sync is definitively NOT claimed.

## Implemented and verified in workspace

- Preserved canonical Mongo/auth/grants/refresh/revocation/role checks, stable IDs, request ledger/attribution, existing galleries/rates/lead flows; no database replacement or identity migration.
- Shared legacy weight/unit adapters; unchanged historical optional fields no longer block photo/title edits. Structured INR labour basis compatibility and display; versioned old/new slab controls and soft deletion.
- Catalog server cap100, stable first/later pages/id tie-breaker, indexed words/exact SKU and filters; separate random discovery; admin FlatList40/page, thumbnails and private image delivery.
- Labelled PDF review/visual move+resize private source-page crop; owner-only page geometry/PNG; same-v1 form export. Fixed preview/mutation lock contention and Back stack duplication. Regenerated sample instructions/manifest.
- New media write intents/byte/hash/purpose ledger, bounded application write budgets, high-watermark UI, lifecycle candidate audit/reference protection, safe byte cache with authorization on every fetch.
- Legacy external HTTPS images render without Authorization headers; only canonical private-media requests receive bearer credentials. Verified with an intercepted external-host test, not a real third-party request.
- Customer registration/login/assignment date+name controls; named query assignee/resolver and daily/range drilldowns; existing canonical admin dashboard plus scoped metrics. Targeted admin customer phone change NOT implemented; self-service remains explicit.

Final suite: **54 passed,0 failed,0 skipped**, including5 authenticated routed mobile-web journeys, `test_reports/pytest/followup_combined_after_races.xml`. Test provider transports are isolated doubles, not real SMS/write proof. `test_reports/followup_final_summary.json` consolidates final status over historical failures. TypeScript/lint/compile passes. Separate read-only managed storage measurement:6 reads succeeded,608598 aggregate bytes, no live upload/deletion.

Final restarted preview paints successfully with no captured render exception. Non-failing warnings remain:9 Python deprecation warnings; React Native Web shadow/pointerEvents deprecations; Metro suggests SDK54 patch alignment for expo-constants18.0.13→18.0.14, expo-font14.0.11→14.0.12 and expo-router6.0.23→6.0.24. These existing dependency patches were not changed as part of this follow-up; review them with native-build acceptance rather than claiming device compatibility from browser success.

10k synthetic metadata test:100pages,10000unique IDs;100-row response62040bytes; current list/search/concurrency timings in `test_reports/catalog_10k_benchmark_iteration16.json`. Mobile-web300-row fixture held40metadata/page; actual scrollTop0→7072,6→14 rendered cards,248→361DOM nodes,reported20.5MB JS heap unchanged; this is not native memory or10k decoded-image proof. PDF60-row subprocess benchmark combined parent+child224.73MiB, not full service peak or production-resource entitlement. See PDF_VALIDATION_EVIDENCE.md and STORAGE_CAPACITY.md.

## Configuration presence (names/status only)

Workspace JWT_SECRET, MSG91_AUTHKEY, ENROLLMENT_INTEGRATION_KEY and existing managed-storage credential are present. Presence does not prove strength, authorization, rotation or delivery. **STAFF_SERVICE_KEY absent**; portal/exchange fail closed. Code now also rejects use of the same value as enrollment key. Target production private secret tooling and website project are not available here; no production or website key configured.

Authorized owner/operator must privately generate a strong separate staff key (at least32random bytes), configure STAFF_SERVICE_KEY on app backend and matching website BFF variable with the same value, send X-Staff-Service-Key only from BFF, rotate/revoke using the maintenance plan, and verify presence/negative isolation/real portal login without publishing values. Enrollment uses ENROLLMENT_INTEGRATION_KEY -> X-Integration-Key. Do not substitute it. CORS allowlist and managed proxy are environment-configured; protected Expo/Mongo variables were not changed. `.env.example` contains names/application safety defaults only.

## Consolidated release gates

| Affected feature | Exact missing input/approval/environment | Completed despite gate |
|---|---|---|
| Repository handoff | User-controlled Save to GitHub; verify resulting NEW SHA and pinned links | Code, five docs, generated OpenAPI, sample/test evidence prepared locally |
| Staff portal + security rotation | Authorized app/website private secret tooling; separate staff key; approved rotation window | Header/role/same-subject/fail-closed guards and tests |
| Genuine SMS | Authorized current target environment/recipient and receipt confirmation; valid MSG91/DLT routing | Canonical purpose-bound OTP/rate-limit/session tests; no real SMS sent this stage |
| Media capacity/delivery | Account-specific included DB/object GB/object/upload limits, outbound/read/write charges, CPU/RAM/concurrency, backups/restore/overages | Read-only inventory/sample projections,6 real reads,10k metadata load, ledger/alerts/cache |
| Remote deletion/privacy | Owner decision: keep Emergent Managed Object Storage (no DELETE API — deletion = access revocation + tombstone, to be described as such in store privacy answers) **or** approve migration to a provider with DELETE (S3/R2/GCS); website/provider erasure/retention acknowledgements | Conservative retained/candidate states, unknown PUT accounting; `provider_delete_supported:false` surfaced; no false deleted flag |
| Production media write/recovery | Explicit approved test budget/object set and production-equivalent credentials; key rotation/readback/checksum/cleanup capability | Isolated write/failure/retry/402/quota tests. NO new live object test uploads |
| Owner reconciliation | Minimal private website+app exports, consistent/restored backup, conflict report and specific hash/per-ID approval | Offline dry-run tool/contract; owner9999813334 target preserved, no9711881372 password admin |
| Play review access | Owner provisions from the production app (Panel → Store review, fresh OTP) or runs `Provision-ReviewAccess.ps1 -Environment production -DbName <DB_NAME> …`; keys go to a private note, then the store forms | `review__` collections in the same database, four roles, hashed reusable keys, owner console + CLI/Windows wrapper with note + deployed-backend verification; preview provisioning/verification end-to-end (12–13 Sep 2026). No universal OTP or published credentials |
| Native acceptance | Appropriate Android/iOS builds and devices/file providers/sharing/background/OS-kill/nav tests | Safe-area/keyboard controls and browser gestures/navigation tested; not native proof |
| Full-service PDF load | Actual production CPU/RAM/concurrency and complete backend+worker+HTTP+authoring/provider test | Current per-page subprocess measurement, configured limits, isolated negative cases |
| Website cutover | Later website implementation reviewed against final contract; both clients staged, backup/admin continuity/secrets/gates cleared; agreed maintenance window | Self-contained handoff and export instructions; no premature rollout |

No production reconciliation, bulk unit migration, provider migration, purchase or billing change was authorized/executed. Current managed storage cannot fulfill required erasure; do not declare production-ready. Source PDFs/chunks/previews remain private even while erasure is blocked.

## UI audit and external deletion boundaries

Implemented controls: registration/last mobile login/last portal login display; account/login/onboarding/search/assignment/date filters; named telecaller assignment; named request filters; age/status/type filters; daily/range attribution drilldown; admin/billing rate permissions; safe-area/keyboard-aware new screens and corrected Back navigation. Optional public preview reload loses in-memory auth by design; native secure-session reopen is a separate unverified device scenario. No admin-targeted phone change is claimed.

Local account deletion/anonymization+tombstones/outbox remains `external_erasure_pending`, not full global deletion. Website drafts/cache/outbox/sessions and provider-held SMS/AI/chat/media/backup retention need scoped acknowledgement and privacy review. Do not answer store Data Safety fields from code alone; source documents, customer fields, requests/rewards, SDK diagnostics and processors must match the actual released binary and operational retention policy. AI Try-On remains removed; existing separate assistant is preserved.